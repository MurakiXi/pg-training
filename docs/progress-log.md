## 2026-06-03

### 完了したこと

- Week1 Day1-4 として、3つの `addEventListener` の重複を観察した。
- `correct-button`、`water-button`、`air-button` の各クリック処理を日本語で分解した。
- 3つの処理で共通している部分と、違っている部分を整理した。
- `reason.textContent = reasonText;` は3つの処理で共通していることを確認した。
- `result.textContent` に入れる文言だけが、正解・不正解で違うことを確認した。
- 現時点では練習のため、関数化や `querySelectorAll` は使わず、ベタ書きのまま進める方針を確認した。
- コード内に、重複を観察するためのコメントを追加した。
- 動作確認では、コメント追加前後で挙動が変わらないことを確認した。

### 学んだこと

- 複数のイベント処理は、まず「同じ部分」と「違う部分」に分けて観察すると整理しやすい。
- 同じ処理が複数回出てきても、初学段階ではすぐに短くせず、まず構造を理解することが重要。
- 後で関数化・配列化・`querySelectorAll` を使うときは、今回見つけた「同じ部分」と「違う部分」が整理の材料になる。

### 詰まった点・注意点

- 「同じ表示欄」という表現は少し曖昧なので、今後は `result` 欄なのか `reason` 欄なのかを明確に書く。
- コメントはコードの動作を変えないが、誤ってコード行を消したり括弧を崩したりすると動作が変わるため、コメント追加後も必ず動作確認する。

### 次にやること

- Week1 Day2 に進む。
- Day2 では、現在ベタ書きしているクリック処理を少し整理する。
- まずは関数を使って、「結果と理由を表示する処理」を1か所にまとめる。
- ただし、まだ配列・オブジェクト・複数問題化には進まない。
- 目的は、`function` を使って同じ処理をまとめる感覚を身につけること。

## 2026-06-04

### 完了したこと

- Week1 Day2 として、結果と理由を表示する処理を `showResult(msg)` 関数にまとめた。
- `showResult(msg)` の中で、`result.textContent` に引数 `msg` を表示し、`reason.textContent` に共通の `reasonText` を表示するようにした。
- 3つの `addEventListener` の中から `showResult("正解！")` または `showResult("残念！")` を呼び出す形に修正した。
- `addEventListener("click", showResult("正解！"))` と書くと、クリック前に `showResult` が実行されてしまうことを確認した。
- `function () { showResult("正解！"); }` の形にすることで、クリック時に処理を実行できるようにした。
- 読み込み直後は結果・理由が表示されず、各ボタンをクリックした時だけ正誤判定と解説が表示されることを確認した。

### 学んだこと

- 関数呼び出し `showResult("正解！")` は、その場で即実行される。
- `addEventListener` の第2引数には、「今すぐ実行した結果」ではなく、「後で実行する関数」を渡す必要がある。
- 引数を使うと、共通処理の中に違う値だけを渡せる。
- 重複していた表示処理を関数にまとめることで、クリック処理の見通しが良くなる。

### 詰まった点・注意点

- `addEventListener("click", showResult("正解！"))` と書くと、クリックを待たずに関数が実行された。
- エラーが出ない場合でも、処理の実行タイミングが間違っていることがある。
- 「関数を呼び出す」と「関数を渡す」は別物として区別する必要がある。

### 次にやること

- Week1 Day2-2 に進む。
- 次は `function () { showResult("..."); }` の形をもう一度別パターンで反復する。
- その後、`showResult` の関数名・引数名・処理内容を自分の言葉で説明する。
- 余裕があれば、ボタン文言を少し変更しても同じ構造で動くか確認する。

## 2026-06-04

### 完了したこと

- Week1 Day2-2 として、`showResult(msg)` 関数の呼び出し時に渡す引数だけを変更した。
- 正解時の表示を「正解！よくできました」に変更した。
- 不正解時の表示を「残念！もう一度考えてみよう」に変更した。
- `showResult` 関数本体を変更せず、引数の変更だけで表示文言が変わることを確認した。

### 学んだこと

- 関数の引数を使うと、同じ処理の中に違う値だけを渡せる。
- 関数本体を変えずに呼び出し側の値を変えることで、表示内容を切り替えられる。
- `msg` は固定の文字ではなく、関数を呼び出す時に渡された値を受け取る入れ物である。

### 詰まった点・注意点

- 関数本体と呼び出し側の役割を分けて考える必要がある。
- 表示文言を変えたいだけなら、共通処理である `showResult` の中身を毎回変更する必要はない。

### 次にやること

- Week1 Day2-3 に進む。
- 次は、`showResult(msg)` の構造を自分の言葉で説明する。
- 「関数名」「引数」「関数本体」「関数呼び出し」の4つを区別する。
- その後、もう一つだけ小変更課題として、理由文も引数で渡せる形にするか検討する。

## 2026-06-04

### 完了したこと

- Week1 Day2-3 として、`showResult(msg)` の構造を自分の言葉で説明した。
- `showResult` は自作関数の名前であることを確認した。
- `msg` は、関数呼び出し時に渡された値を受け取るための引数であることを確認した。
- `result.textContent = msg;` は、正誤判定エリアの表示文言を引数の値に書き換える処理であることを確認した。
- `reason.textContent = reasonText;` は、理由表示欄に共通の解説文を表示する処理であることを確認した。
- `addEventListener("click", function () { showResult("..."); })` と書く理由を整理した。

### 学んだこと

- `showResult("正解！")` は、その場で即実行される。
- `showResult` だけを渡す場合は即実行ではないが、今回のように任意の文言を引数として渡すには不十分。
- `function () { showResult("正解！"); }` と書くことで、クリック時に指定した引数つきで関数を実行できる。
- 関数名、引数、関数本体、関数呼び出しは分けて考える必要がある。

### 詰まった点・注意点

- 「関数を渡す」と「関数を呼び出す」は別物。
- `()` が付くと、その場で関数呼び出しになる。
- `addEventListener` の第2引数には、後で実行する処理を渡す必要がある。

### 次にやること

- Week1 Day2-4 に進む。
- 次は、理由文も引数で渡せるように `showResult` を少し拡張する。
- 目的は、関数に複数の値を渡す感覚を身につけること。

## 2026-06-04

### 完了したこと

- Week1 Day2-4 として、`showResult` 関数に2つの引数を渡す形へ修正した。
- `showResult(msg, explanation)` を作成し、`msg` を `result.textContent` に、`explanation` を `reason.textContent` に表示するようにした。
- 各 `addEventListener` の中で、結果文と理由文を引数として `showResult` に渡した。
- 電気・水・空気の各ボタンをどの順序でクリックしても、結果文と理由文が正しく表示されることを確認した。

### 学んだこと

- 関数には複数の引数を渡せる。
- 複数の引数は、呼び出し側と関数定義側で順番が対応する。
- `showResult("正解！", reasonText)` と呼び出すと、1つ目の値が `msg` に入り、2つ目の値が `explanation` に入る。
- 関数に値を渡すことで、関数本体を変更せずに表示内容を変えられる。

### 詰まった点・注意点

- 複数引数では、順番を間違えると結果欄と理由欄の表示が入れ替わる。
- カンマ後のスペースや文末セミコロンなど、書式を揃えると読みやすくなる。
- 動作確認だけでなく、読み込み直後に余計な表示が出ていないかも確認する。

### 次にやること

- Week1 Day3 に進む。
- Day3では、複数の不正解ボタンに似た処理を設定するため、`querySelectorAll` と `forEach` の入口を学ぶ。
- 目的は、複数のDOM要素をまとめて取得し、1つずつ処理する感覚を身につけること。
- まだ配列・オブジェクトによる複数問題化には進まない。

## 2026-06-05

### 完了したこと

- Week1 Day3-2 として、`querySelectorAll` と `forEach` の理解確認を行った。
- `querySelector` は条件に一致する最初の要素を1つ取得し、`querySelectorAll` は条件に一致する要素すべてをまとめて取得することを説明した。
- `querySelectorAll` で取得した `NodeList` は要素の集まりであり、そのまま `addEventListener` を設定できないことを確認した。
- `forEach(function (incorrectButton) { ... })` の `incorrectButton` は、`NodeList` から1つずつ取り出された現在の要素であることを確認した。
- `addEventListener("click", function () { ... })` の中の関数に引数がなくてもよい理由を確認した。
- 不正解ボタンを1つ追加し、JavaScriptを変更せずに同じ不正解処理が動くことを確認した。

### 学んだこと

- `querySelectorAll` は複数要素の集まりを返す。
- 複数要素の集まりには、直接イベントを設定するのではなく、`forEach` で1つずつ取り出して設定する。
- `forEach` の引数は、現在取り出されている1つの要素を受け取るための名前である。
- `addEventListener` のコールバック関数には、必要なら `event` を受け取れるが、今回は使わないので空でよい。
- class を共通化しておくと、同じ種類のボタンを増やしてもJavaScript側を変更せずに同じ処理を適用できる。

### 詰まった点・注意点

- `addEventListener(...)` とだけ書くと、意図したボタンではなく別の対象にイベントが登録される可能性がある。
- `incorrectButton.addEventListener(...)` のように、「どの要素にイベントを付けるのか」を明示する必要がある。
- JavaScriptでは `showResult` と `ShowResult` のような大文字小文字の違いも別名として扱われる。

### 次にやること

- Week1 Day3-3 に進む。
- 次は、`addEventListener` のコールバック関数に `event` を受け取り、クリックされたボタンの情報を確認する。
- 目的は、`event.target` を使って「どのボタンがクリックされたか」を確認する感覚を身につけること。
- ただし、まだ配列・オブジェクト・複数問題化には進まない。

## 2026-06-05

### 完了したこと

- Week1 Day3-3 として、`addEventListener` のコールバック関数で `event` を受け取る練習をした。
- `console.log(event)` でクリックイベント全体の情報を確認した。
- `console.log(event.target)` で、実際にクリックされたボタン要素だけを確認した。
- 水・空気・光の各不正解ボタンをクリックすると、それぞれのボタン要素が `event.target` として表示されることを確認した。
- 正解ボタンと不正解ボタン群の処理対象が分かれていることを確認した。

### 学んだこと

- `event` には、クリックに関する多くの情報が入っている。
- `event.target` は、実際にクリックされた要素を指す。
- `forEach` で不正解ボタン1つ1つにイベントを設定すると、それぞれのクリック時に個別の `event.target` を確認できる。
- `console.log` は、処理の流れや取得できている値を確認するために有効。

### 詰まった点・注意点

- `event` を使う必要がない場合は、コールバック関数の引数は空でよい。
- `event.target` を見る時は、どのボタンをクリックした結果なのかを取り違えないようにする。
- 学習用の `console.log` は便利だが、完成版では削除またはコメントアウトする。

### 次にやること

- Week1 Day3-4 に進む。
- 次は、`event.target.textContent` を使って、クリックされたボタンの表示文字を確認する。
- 目的は、クリックされた要素そのものだけでなく、その要素が持つテキストを取得する感覚を身につけること。
- まだ `if` による正誤判定や複数問題化には進まない。

## 2026-06-05

### 完了したこと

- Week1 Day3-4 として、`event.target.textContent` を使い、クリックされたボタンの表示文字を確認した。
- 水・空気・光の各不正解ボタンをクリックした時、Console にそれぞれの文字が表示されることを確認した。
- `showResult` の正誤表示・理由表示がこれまで通り機能していることを確認した。
- 読み込み直後に、正誤判定エリアと解説エリアへ余計な表示が出ないことを確認した。

### 学んだこと

- `event.target` は、実際にクリックされた要素を指す。
- `event.target.textContent` を使うと、その要素に表示されている文字を取得できる。
- `querySelectorAll` と `forEach` で複数ボタンに同じイベント処理を設定しても、クリックされたボタンごとの情報は `event.target` で確認できる。
- クリックされた選択肢の文字を取得できると、今後の正誤判定処理につなげられる。

### 詰まった点・注意点

- `event.target.textContent` は、変数名ではなく実際にクリックされたDOM要素の表示文字を見ている。
- 学習用の `console.log` は便利だが、完成版では削除またはコメントアウトする。
- `event.target.textContent` の取得結果には、HTMLの改行や空白が影響する場合があるため、今後必要に応じて `trim()` を使う可能性がある。

### 次にやること

- Week1 Day4 に進む。
- Day4では、クリックされた選択肢の文字を使って処理を分ける。
- 具体的には、正解・不正解をボタンごとに固定するのではなく、クリックされた文字と正解文字を比較する入口に進む。
- ただし、まだ複数問題化や配列・オブジェクト化には進まない。

## 2026-06-05

### 完了したこと

- Week1 Day4-1 として、クリックされた選択肢の文字を使って正誤判定する処理を実装した。
- すべての選択肢ボタンに共通 class `answer-button` を付けた。
- `querySelectorAll(".answer-button")` で選択肢ボタンをまとめて取得した。
- `forEach` で各ボタンに `addEventListener` を設定した。
- `event.target.textContent` でクリックされたボタンの表示文字を取得した。
- 正解文字を `const answer = "電気"` として用意し、クリックされた文字と比較した。
- `if / else` を使い、一致した場合は「正解！」、一致しない場合は「残念！」を `showResult` に渡した。
- ボタンの順序を変えても、電気ボタンだけが正解判定になることを確認した。

### 学んだこと

- ボタンごとに正解・不正解を固定するのではなく、クリックされた文字と正解文字を比較して判定できる。
- `event.target.textContent` は、クリックされたボタンの表示文字を取得する。
- `if` を使うと、条件に応じて処理を分けられる。
- 共通 class と `querySelectorAll` を使うと、ボタンの個数や順序に依存しにくい処理を書ける。

### 詰まった点・注意点

- `event.target.textContent` に余分な空白や改行が含まれると、見た目は同じでも文字列比較に失敗する可能性がある。
- `answer` という変数名でも動作するが、今後は `correctAnswer` のように役割が明確な名前も検討する。
- 文末セミコロンやカンマ後のスペースなど、書式を揃えると読みやすくなる。

### 次にやること

- Week1 Day4-2 に進む。
- 次は `trim()` を使い、`event.target.textContent` の前後の空白を取り除いてから比較する。
- 目的は、HTMLの改行や余白によって正誤判定が崩れる可能性を理解し、防ぐこと。
- その後、Day4を完了扱いにしてよいか判定する。

## 2026-06-05

### 完了したこと

- Week1 Day4-2 として、`trim()` を使ってクリックされたボタン文字の前後の空白・改行を取り除く処理を実装した。
- HTML側で電気ボタン内に改行と空白を入れ、`trim()` なしでは「残念！」になることを確認した。
- `event.target.textContent.trim()` に修正し、改行や空白があっても「電気」が正解判定になることを確認した。

### 学んだこと

- `textContent` は、見た目の文字だけでなくHTML内の改行や空白も含む場合がある。
- `trim()` は文字列の前後の空白・改行を取り除く。
- 見た目が同じでも、文字列比較では余計な空白が原因で不一致になることがある。
- 壊れる状態を先に作ってから、修正で直ることを確認すると、処理の意味が分かりやすい。

### 詰まった点・注意点

- `trim()` は前後の空白を取り除くが、文字列の途中の空白までは消さない。
- `event.target.textContent.trim()` とすることで、正解文字との比較前に文字を整えられる。
- HTMLの改行・インデントは、表示だけでなく取得される文字列にも影響する場合がある。

### 次にやること

- Week1 Day4-3 に進む。
- 次は、変数名とコード整形を行う。
- `answer` という変数名を `correctAnswer` に変更する。
- セミコロン、インデント、カンマ後スペースを揃える。
- 学習用の `console.log` を残すか削除するか判断する。
- その後、Day4完了扱いにできるか判定する。

## 2026-06-05

### 完了したこと

- Week1 Day4-3 として、変数名とコード書式を整理した。
- 正解文字を表す変数名を `answer` から `correctAnswer` に変更した。
- `querySelectorAll(".answer-button")` で全選択肢ボタンを取得し、`forEach` で各ボタンにクリックイベントを設定した。
- `event.target.textContent.trim()` でクリックされた選択肢の文字を取得し、前後の空白・改行を取り除いた。
- `correctAnswer` とクリックされた文字を比較し、`if / else` で正解・不正解を判定した。
- `showResult(msg, explanation)` に結果文と解説文を渡し、正誤判定エリアと解説エリアを更新した。
- 読み込み直後は結果・解説が表示されず、ボタンをクリックした時だけ正誤と解説が表示されることを確認した。

### 学んだこと

- 変数名は、値の役割が分かる名前にするとコードが読みやすくなる。
- `correctAnswer` のように具体的な名前を使うと、「正解文字」として使う値だと分かりやすい。
- `event.target.textContent.trim() === correctAnswer` の形で、クリックされた選択肢の文字と正解文字を比較できる。
- `if / else` を使うと、条件に応じて表示する文言を切り替えられる。
- 動作を変えずに、命名・書式・不要な確認コードを整理することも重要な作業である。

### 詰まった点・注意点

- 文字列比較では、HTML内の改行や空白が影響する場合があるため、必要に応じて `trim()` を使う。
- 変数名を変更した場合は、定義箇所だけでなく参照箇所もすべて変更する必要がある。
- シングルクォートとダブルクォートは、動作上はどちらでもよいが、プロジェクト内では統一した方が読みやすい。

### 次にやること

- Week1 Day5 として、Week1の総復習を行う。
- 新しい小4理科の一問クイズを、できるだけ自力で作る。
- 使う技術は、共通 class、`querySelectorAll`、`forEach`、`addEventListener`、`event.target.textContent.trim()`、`if / else`、`showResult`。
- 目的は、既存コードを少し変えるだけでなく、同じ構造を別題材で再現できるか確認すること。
- Day5が完了したら、Week2の配列・オブジェクトによる複数問題化へ進む。

## 2026-06-05

### 完了したこと

- Week1 Day5 として、別題材の一問クイズを自力で再実装した。
- 小4理科「水のすがた」を題材に、水を熱した時に何になるかを問うクイズを作成した。
- HTMLに問題文、選択肢ボタン、正誤判定エリア、解説エリアを用意した。
- 全選択肢ボタンに共通 class `answer-button` を付けた。
- JavaScriptで `querySelectorAll(".answer-button")` を使い、全選択肢ボタンをまとめて取得した。
- `forEach` で各ボタンに `addEventListener` を設定した。
- `event.target.textContent.trim()` でクリックされた選択肢の文字を取得した。
- `correctAnswer` とクリックされた文字を比較し、`if / else` で正解・不正解を判定した。
- `showResult(judge, explanation)` により、正誤判定エリアと解説エリアを更新した。
- 正解ボタンの順序を変えても、水蒸気だけが正解になることを確認した。
- 水蒸気ボタン内に空白や改行があっても、`trim()` により正しく正解判定されることを確認した。

### 学んだこと

- Week1で学んだDOM操作・イベント処理・関数・条件分岐を、別題材でも再現できる。
- 共通 class と `querySelectorAll` を使うと、ボタンの順序や個数に依存しにくい処理になる。
- `event.target.textContent.trim()` と `correctAnswer` の比較により、クリックされた選択肢の文字で正誤判定できる。
- `showResult` のような表示用関数を作ると、結果表示と解説表示をまとめて扱える。
- 動作確認では、通常のクリックだけでなく、ボタン順序変更や空白・改行の影響も確認するとよい。

### 詰まった点・注意点

- `script2.js` を使う場合は、今後の管理上その理由を明確にする。基本的には `script.js` に統一した方が分かりやすい。
- `showResult("正解！", reasonText);` のように、文末セミコロンを揃える。
- 子供向け解説文は、正確さだけでなく長さと読みやすさも考える必要がある。

### 次にやること

- Week1を完了扱いとし、Week2へ進む。
- Week2では、問題データを配列・オブジェクトとして扱う。
- 最初の課題は、1問分の問題文・選択肢・正解・解説をオブジェクトにまとめること。
- その後、複数の問題オブジェクトを配列に入れ、現在の問題を表示する処理へ進む。

## 2026-06-08

### 完了したこと

- Week2 Day1 として、1問分のクイズデータをオブジェクトにまとめた。
- `questionData` に `statement`、`correctAnswer`、`reasonText` を持たせた。
- `questionData.statement` を使って問題文を表示した。
- `questionData.correctAnswer` を使って正誤判定した。
- `questionData.reasonText` を使って解説文を表示した。
- 当初、`questionData` を配列として作成してしまい、`questionData.statement` などが `undefined` になる問題を確認した。
- 外側を `[]` ではなく `{}` にすることで、1問分のデータを1つのオブジェクトとして扱えるように修正した。

### 学んだこと

- `{}` は1つのオブジェクトを作る。
- `[]` は複数の値を並べる配列を作る。
- `questionData.statement` のようなドット記法は、オブジェクトが直接持っているプロパティを取り出す。
- 配列の中に複数のオブジェクトを入れた場合、`questionData.statement` では直接取り出せない。
- 1問分の情報は、まず1つのオブジェクトとしてまとめると扱いやすい。

### 詰まった点・注意点

- オブジェクトを作るつもりで、誤って配列を作っていた。
- `console.log(questionData)` で構造を見ると、配列なのかオブジェクトなのか確認できる。
- データが作れていても、目的に合った構造でなければ `undefined` になり、画面表示や判定に失敗する。

### 次にやること

- Week2 Day2 に進む。
- 次は、選択肢も `questionData` の中にまとめる。
- ただし、最初はボタンをJavaScriptで生成するところまでは急がず、まずは `choice1`〜`choice4` または `choices` のどちらで持つべきかを検討する。
- 目的は、問題文・正解・解説だけでなく、選択肢もデータとして扱う感覚を身につけること。

## 2026-06-08

### 完了したこと

- Week2 Day2-1 として、選択肢を `questionData` の中に追加した。
- `questionData.choice1`、`questionData.choice2`、`questionData.choice3`、`questionData.choice4` を作成した。
- HTML側の選択肢ボタンの中身を空にした。
- `answerButtons[0].textContent = questionData.choice1;` のように、JavaScript側から各ボタンに選択肢を表示した。
- HTMLに選択肢を直書きしなくても、画面上に選択肢が表示されることを確認した。
- クリック時の正誤判定と解説表示が、これまで通り正しく動くことを確認した。

### 学んだこと

- `querySelectorAll` で取得した複数要素は、`answerButtons[0]`、`answerButtons[1]` のように添字で取り出せる。
- 添字は0から始まるため、1個目のボタンは `[0]`、4個目のボタンは `[3]` になる。
- `textContent` を使うと、JavaScript側のデータをHTML要素の表示文字として入れられる。
- 選択肢をHTML直書きではなくデータから表示することで、後から問題データを差し替えやすくなる。

### 詰まった点・注意点

- HTML側のボタンを空にしても、JavaScriptから `textContent` を入れれば画面に選択肢を表示できる。
- 空のボタンを書く時は、不要な空白を入れず `<button class="answer-button"></button>` の形に揃えると読みやすい。
- `answerButtons[3]` を使うには、HTML側に `.answer-button` を持つボタンが4つ存在している必要がある。

### 次にやること

- Week2 Day2-2 に進む。
- 次は、`choice1`〜`choice4` のような個別プロパティではなく、`choices` というまとまりで選択肢を持つ形を学ぶ。
- 目的は、選択肢を「個別の値4つ」ではなく「選択肢の集まり」として扱う感覚を身につけること。
- ただし、まだ複数問題化や `createElement` によるボタン生成には進まない。

## 2026-06-09

### 完了したこと

- Week2 Day2-2 として、選択肢を `choices` 配列にまとめた。
- `questionData` 内の `choice1`〜`choice4` を削除した。
- `questionData` に `choices: ["氷", "水蒸気", "砂", "光"]` を追加した。
- `questionData.choices[0]`〜`questionData.choices[3]` を使って、各ボタンに選択肢を表示した。
- HTML側のボタン文字を空にしたまま、JavaScript側のデータから選択肢を表示できることを確認した。
- 水蒸気を押すと正解、氷・砂・光を押すと不正解になることを確認した。

### 学んだこと

- 配列は、同じ種類の値を順番にまとめる時に使える。
- 選択肢4つは、`choice1`〜`choice4` よりも `choices` 配列としてまとめた方が扱いやすい。
- 配列の添字は0から始まる。
- `questionData.choices[0]` は、`choices` 配列の1個目の値を取り出す。
- `answerButtons[0]` と `questionData.choices[0]` を対応させることで、1個目のボタンに1個目の選択肢を表示できる。

### 詰まった点・注意点

- 配列の1個目は `[1]` ではなく `[0]`。
- `answerButtons[0]` はボタン要素、`questionData.choices[0]` は表示したい文字列。
- 今は4つのボタンに対して4行で表示しているが、同じ形の処理が繰り返されている。

### 次にやること

- Week2 Day2-3 に進む。
- 次は、`answerButtons[0]`〜`answerButtons[3]` に1つずつ代入している処理を、`forEach` を使ってまとめる。
- 目的は、ボタンの数と選択肢の数が対応している場合に、添字を使って繰り返し処理を書く感覚を身につけること。

## 2026-06-09

### 完了したこと

- Week2 Day2-3 として、選択肢表示の4行の手書き代入を `forEach` に置き換えた。
- `answerButtons.forEach(function (answerButton, index) { ... })` の形で、ボタンを1つずつ取り出した。
- `index` を使って、現在のボタンと `questionData.choices[index]` を対応させた。
- `answerButton.textContent = questionData.choices[index];` により、選択肢を繰り返し処理で表示できるようにした。
- Day2-2 と同じ表示・正誤判定・解説表示が動くことを確認した。

### 学んだこと

- `forEach` の第1引数には、現在処理中の要素が入る。
- `forEach` の第2引数には、現在処理中の要素の添字 `index` が入る。
- `index` は自分で数える必要はなく、`forEach` が渡してくれる。
- `answerButtons[index]` と `questionData.choices[index]` のように、同じ添字を使うことで、DOM要素と配列データを対応させられる。
- 同じ形の処理が並んでいる場合、`forEach` でまとめられる可能性がある。

### 詰まった点・注意点

- `index` は特別な予約語ではなく、引数名なので別名にもできる。
- ただし、慣れるまでは `index` と書くのが分かりやすい。
- `answerButton` はボタン要素、`questionData.choices[index]` は文字列であり、役割が違う。
- `forEach` は「配列やNodeListの中身を1つずつ取り出して処理する」ためのもの。

### 次にやること

- Day2-4へ進む前に、`forEach` と `index` のミニ復習を行う。
- 復習後、次は選択肢表示用の処理を関数にまとめるか、複数問題化の入口へ進むかを判断する。

### ミニ復習

- `forEach` の第1引数には現在処理中の要素が入る。
- `forEach` の第2引数には現在処理中の添字 `index` が入る。
- `answerButton` はDOM要素、`questionData.choices[index]` は表示する文字列。
- `index` を使うことで、ボタンと選択肢配列の同じ順番の要素を対応させられる。

## 2026-06-09

### 完了したこと

- Week2 Day2-4 として、問題文と選択肢を表示する処理を `renderQuestion` 関数にまとめた。
- `renderQuestion(question)` の形で、1問分のデータを引数として受け取る関数を作成した。
- 関数内で `question.statement` を使って問題文を表示した。
- 関数内で `question.choices[index]` を使って、各ボタンに選択肢を表示した。
- 関数の外で `renderQuestion(questionData)` を呼び出し、これまでと同じ挙動になることを確認した。

### 学んだこと

- オブジェクトを関数の引数として渡すと、関数内でそのデータを使って画面表示を更新できる。
- `questionData` を直接関数内で使うのではなく、引数 `question` として受け取ることで、より再利用しやすい関数になる。
- `renderQuestion(questionData)` のように、データを渡して表示を切り替える形は、今後の複数問題化につながる。
- 問題文表示と選択肢表示のように、まとまった目的を持つ処理は関数にまとめられる。

### 詰まった点・注意点

- `renderQuestion(statement, correctAnswer, reasonText, choices)` のようにバラバラに渡すより、`questionData` を1つのまとまりとして渡す方がよい。
- 関数内では、固定の `questionData` ではなく、引数として受け取った `question` を使うと汎用性が上がる。
- `forEach` の中の処理は、複数行で書くと読みやすい。

### 次にやること

- Week2 Day2-5 に進む。
- 次は、クリック時の正誤判定も関数に整理するか、現在の `questionData` を複数問題化しやすい形へ発展させる準備を行う。
- 目的は、「問題を表示する処理」と「回答を判定する処理」の役割を分けて理解すること。

## 2026-06-09

### 完了したこと

- Week2 Day2-5 として、回答判定処理を `isCorrectAnswer` 関数に分けた。
- `isCorrectAnswer(answerText, question)` の形で、クリックされた文字列と問題データを受け取る関数を作成した。
- 関数内で `answerText === question.correctAnswer` を比較し、正解なら `true`、不正解なら `false` を返すようにした。
- イベントリスナー内の `if` 文で `isCorrectAnswer` の戻り値を使い、正解・不正解の表示を分岐した。
- ブラウザ上で、これまでと同じ挙動になることを確認した。

### 学んだこと

- 関数は、画面表示だけでなく「判定処理」も分けて持たせられる。
- `return true` / `return false` により、関数の外へ判定結果を返せる。
- `if (isCorrectAnswer(...))` のように、関数の戻り値をそのまま条件分岐に使える。
- イベントリスナー内にすべてを書くのではなく、処理ごとに関数へ分けると読みやすくなる。

### 詰まった点・注意点

- `event.judgement` のような値は自動では存在しない。
- 判定結果は、自作関数 `isCorrectAnswer` の戻り値として作る。
- `answerText` を文字列として扱う場合、関数内では `textContent` は使わない。
- クリックされた文字列を取り出す処理と、正解かどうかを判定する処理は分けて考える。

### 次にやること

- Week2 Day2-6 に進む。
- 次は、イベントリスナー内で `const answerText = event.target.textContent.trim();` のように変数を使い、クリック時処理を読みやすく整理する。
- 目的は、イベントリスナーを「文字を取り出す → 判定する → 結果を表示する」という流れで読める形にすること。

## 2026-06-09

### 完了したこと

- Week2 Day2-6 として、イベントリスナー内のクリック時処理を整理した。
- クリックされたボタンの文字列を `const answerText` に入れる形にした。
- `isCorrectAnswer(answerText, questionData)` の戻り値を `const isCorrect` に入れる形にした。
- `if (isCorrect)` を使って、正解・不正解の表示を分岐した。
- ブラウザ上で、これまでと同じ挙動になることを確認した。

### 学んだこと

- イベントリスナー内では、「文字を取り出す」「判定する」「結果を表示する」という順番で処理を整理できる。
- `event.target.textContent.trim()` によって、実際にクリックされたボタンの文字列を取り出せる。
- `isCorrectAnswer(...)` の戻り値を `isCorrect` という変数に入れると、if文の意図が読みやすくなる。
- 関数の戻り値 `true` / `false` は、変数に入れてから条件分岐に使える。

### 詰まった点・注意点

- `if (isCorrectAnswer(...))` のままでも動くが、`answerText` と `isCorrect` に分けると処理の読み筋が明確になる。
- `answerText` は文字列、`isCorrect` は真偽値であり、役割が違う。
- イベントリスナーの中にすべてを直接書くのではなく、変数と関数を使って小さな処理に分けると読みやすくなる。

### 次にやること

- Week2 Day3 に進む。
- 次は、1問だけの `questionData` から、複数の問題データを扱う準備に入る。
- まずは、複数の問題オブジェクトを配列に入れる考え方を学ぶ。
- 目的は、「1問分のオブジェクト」から「問題一覧の配列」へ発展させること。

## 2026-06-10

### 完了したこと

- Week2 Day3-1 として、1問分の `questionData` を、複数問題を入れられる `questions` 配列へ変更した。
- `questions` 配列の中に、問題オブジェクトを2つ入れた。
- 1問目として「水蒸気」の問題を入れた。
- 2問目として「電気」の問題を入れた。
- `renderQuestion(questions[0])` の形で、配列の0番目の問題を表示した。
- 正誤判定でも `isCorrectAnswer(answerText, questions[0])` を使った。
- 解説表示でも `questions[0].reasonText` を使った。
- ブラウザ上で、これまでと同じ挙動になることを確認した。

### 学んだこと

- 複数の問題データは、配列の中にオブジェクトを入れる形で管理できる。
- `questions[0]` は、`questions` 配列の1問目のオブジェクトを表す。
- `questions[1]` は、2問目のオブジェクトを表す。
- 表示・判定・解説で同じ問題データを使う必要がある。
- 表示に `questions[0]` を使うなら、判定と解説も `questions[0]` に揃える必要がある。

### 詰まった点・注意点

- 外側の `[]` は配列、内側の `{}` は1問分のオブジェクト。
- オブジェクト内では、プロパティ名と値を `:` でつなぐ。
- プロパティ同士は `,` で区切る。
- `questions[0]` を直書きしている状態では、まだ1問目しか表示できない。

### 次にやること

- Week2 Day3-2 に進む。
- 次は、`questions[0]` を直接書くのではなく、現在の問題番号を表す変数を使う。
- 目的は、`currentQuestionIndex` のような変数を使って、表示する問題を切り替える準備をすること。

## 2026-06-10

### 完了したこと

- Week2 Day3-2 として、現在の問題番号を表す `currentQuestionIndex` を作成した。
- `let currentQuestionIndex = 0;` として、初期値を0にした。
- `questions[0]` を直接使っていた箇所を、`questions[currentQuestionIndex]` に置き換えた。
- `renderQuestion(questions[currentQuestionIndex])` の形で、現在の問題を表示した。
- 正誤判定でも `isCorrectAnswer(answerText, questions[currentQuestionIndex])` を使った。
- 解説表示でも `questions[currentQuestionIndex].reasonText` を使った。
- ブラウザ上で、これまでと同じ挙動になることを確認した。

### 学んだこと

- `currentQuestionIndex` のような変数を使うと、「今どの問題を表示しているか」を管理できる。
- `questions[currentQuestionIndex]` は、現在の問題番号に対応する問題オブジェクトを表す。
- `currentQuestionIndex` が0なら `questions[0]`、1なら `questions[1]` を参照する。
- 表示・判定・解説で同じ `questions[currentQuestionIndex]` を使うことで、現在の問題に処理を揃えられる。

### 詰まった点・注意点

- `currentQuestionIndex` は後で値を変える予定があるため、`const` ではなく `let` で宣言する。
- まだ `currentQuestionIndex++` は行わない。
- 今回は見た目の挙動を変える課題ではなく、後で問題を切り替えるための準備である。

### 次にやること

- Week2 Day3-3 に進む。
- 次は、`currentQuestionIndex` の値を手動で `1` に変えて、2問目が表示されることを確認する。
- 目的は、`currentQuestionIndex` の値によって、表示・判定・解説に使われる問題が切り替わることを理解すること。

### Week2 Day3-3 ミニ確認

- `currentQuestionIndex` を一時的に `0` から `1` に変更した。
- `currentQuestionIndex = 1` の時、`questions[1]` の2問目が表示されることを確認した。
- 2問目の正解・不正解判定と解説表示が正しく動くことを確認した。
- `currentQuestionIndex` を `0` に戻すと、表示問題も1問目に戻ることを確認した。
- `questions[currentQuestionIndex]` は、現在の問題番号に対応する問題データを取り出す書き方だと理解した。

## 2026-06-10

### 完了したこと

- Week2 Day3-4 として、「次の問題」ボタンを追加した。
- HTMLに `id="next-button"` を持つボタンを追加した。
- JavaScriptで `querySelector` を使い、「次の問題」ボタンを取得した。
- 「次の問題」ボタンに `click` イベントを設定した。
- ボタンを押した時に `currentQuestionIndex = 1;` として、2問目を表示するようにした。
- `renderQuestion(questions[currentQuestionIndex])` を再実行し、現在の問題番号に対応する問題を表示した。
- 問題を切り替えた時に、`result.textContent = "";` と `reason.textContent = "";` で前問の結果・解説を消すようにした。
- ページ読み込み直後の1問目、次の問題ボタンクリック後の2問目がどちらも正しく動作することを確認した。

### 学んだこと

- ボタンのクリックイベント内で変数の値を変更すると、表示する問題を切り替えられる。
- `currentQuestionIndex` の値を変えた後、`renderQuestion` を呼び直すことで画面表示を更新できる。
- 画面に表示されている内容は、データの値を変えただけでは自動で変わらない。
- データを変えた後に、表示処理をもう一度呼ぶ必要がある。
- 問題を切り替える時は、前問の結果や解説を消す必要がある。

### 詰まった点・注意点

- 今回は `currentQuestionIndex = 1;` と固定しているため、「必ず2問目へ行く」処理になっている。
- まだ「次の問題へ順番に進む」処理ではない。
- 変数名は `nextbutton` でも動くが、JavaScriptでは `nextButton` のようなキャメルケースが読みやすい。

### 次にやること

- Week2 Day3-5 に進む。
- 次は、`currentQuestionIndex = 1;` の固定指定をやめて、現在の問題番号を1つ進める処理にする。
- 目的は、「次の問題」ボタンを押すたびに、現在の問題番号を更新する感覚を身につけること。

## 2026-06-10

### 完了したこと

- Week2 Day3-5 として、「次の問題」ボタンの処理を変更した。
- `currentQuestionIndex = 1;` のように固定で2問目へ移動する処理をやめた。
- `currentQuestionIndex++` を使い、現在の問題番号を1つ進める形にした。
- 問題番号を進めた後、`renderQuestion(questions[currentQuestionIndex])` を呼び、次の問題を表示した。
- 1問目から2問目へ進む挙動が、これまで通り正しく動くことを確認した。

### 学んだこと

- `currentQuestionIndex++` は、現在の数値を1つ増やす処理である。
- 固定値を代入する処理と、現在の値を更新する処理は意味が違う。
- 問題番号を更新した後、画面表示を変えるには `renderQuestion` をもう一度呼ぶ必要がある。
- `currentQuestionIndex` は、現在どの問題を表示しているかを表す状態変数として使える。

### 詰まった点・注意点

- 今回はまだ、最後の問題でさらに「次の問題」を押した場合の対策はしていない。
- 2問目でさらにボタンを押すと、存在しない `questions[2]` を参照して不具合が起きる可能性がある。
- 次の課題では、この範囲外アクセスを防ぐ必要がある。

### 次にやること

- Week2 Day3-6 に進む。
- 次は、最後の問題でさらに「次の問題」を押した時に、存在しない問題へ進まないようにする。
- 目的は、配列の範囲外を参照しないための条件分岐を学ぶこと。

## 2026-06-10

### 完了したこと

- Week2 Day3-6 として、最後の問題より先へ進まないようにした。
- `questions.length` を使って、問題数を取得した。
- 最後の問題番号は `questions.length - 1` で表せることを確認した。
- 「次の問題」ボタンの処理に `if` 文を追加した。
- `currentQuestionIndex < num - 1` の条件を使い、最後の問題でさらに進まないようにした。
- 2問目で「次の問題」ボタンを押しても、画面が壊れず、開発者ツールにもエラーが出ないことを確認した。

### 学んだこと

- 配列の要素数は `配列.length` で取得できる。
- 配列の添字は0から始まるため、最後の添字は `length - 1` になる。
- 問題数が2問の場合、存在する添字は `0` と `1` であり、`2` は存在しない。
- 次へ進む前に条件分岐を入れることで、存在しないデータを参照する事故を防げる。

### 詰まった点・注意点

- `currentQuestionIndex < questions.length` だと、最後の問題からさらに進めてしまう。
- 「問題数」と「最後の添字」は1ずれる。
- `num` でも動くが、後で読み返しやすくするなら `questionCount` のような名前も候補になる。

### 次にやること

- Week2 Day3-7 に進む。
- 次は、最後の問題で「次の問題」ボタンを押した時に、何も起こらないだけでなく、ユーザーに分かる表示を出す。
- 目的は、条件分岐の `else` 側を使って、最後の問題に到達した時の処理を考えること。

## 2026-06-10

### 完了したこと

- Week2 Day3-7 として、最後の問題で「次の問題」ボタンを押した時の処理を追加した。
- `nextButton` のクリックイベント内に `else` を追加した。
- 次の問題がある場合は、これまで通り `currentQuestionIndex` を進めて問題を切り替えるようにした。
- 次の問題がない場合は、`reason` エリアに「最後の問題です」と表示するようにした。
- 2問目でさらに「次の問題」ボタンを押しても、画面が壊れず、Consoleにエラーが出ないことを確認した。

### 学んだこと

- `if` は条件を満たす場合の処理、`else` は条件を満たさない場合の処理を書ける。
- 「次の問題がある場合」と「次の問題がない場合」を分けることで、範囲外アクセスを防ぎつつ、ユーザーに状態を伝えられる。
- エラーを防ぐだけでなく、画面上に適切なメッセージを出すこともアプリの大事な処理である。

### 詰まった点・注意点

- 最後の問題で `currentQuestionIndex` を進めてしまうと、存在しない問題を参照してしまう。
- `else` 側では `renderQuestion` を呼ばず、現在の問題表示を維持する。
- 今回は `reason` エリアに最後の問題であることを表示した。

### 次にやること

- Week2 Day3-8 に進む。
- 次は、問題を切り替えた時に結果表示と解説表示をリセットする処理を関数にまとめる。
- 目的は、`result.textContent = "";` と `reason.textContent = "";` のような表示リセット処理を、意味のある関数名で扱えるようにすること。

## 2026-06-10

### 完了したこと

- Week2 Day3-8 として、結果表示と解説表示を消す処理を関数にまとめた。
- `clearFeedback()` 関数を作成した。
- `clearFeedback()` の中で、`result.textContent = "";` と `reason.textContent = "";` を実行するようにした。
- 「次の問題」ボタンで次の問題へ進んだ時に、`clearFeedback()` を呼び出すようにした。
- 次の問題へ進んだ時、前問の「正解！」「残念！」や解説文が消えることを確認した。
- 最後の問題で「次の問題」ボタンを押した時の「最後の問題です」は、これまで通り表示されることを確認した。

### 学んだこと

- 同じ意味を持つ表示処理は、関数にまとめると読みやすくなる。
- `clearFeedback()` のように関数名を付けることで、「何をしている処理か」がコード上で分かりやすくなる。
- 問題を切り替える処理と、結果・解説を消す処理を分けて考えられる。
- 関数を呼ぶ場所によって、画面上の挙動が変わる。

### 詰まった点・注意点

- `clearFeedback()` は、次の問題へ進めた時だけ呼ぶ。
- 最後の問題で押した時に `clearFeedback()` を呼ぶと、「最後の問題です」という表示まで消えてしまう。
- `else` 側では、現在の問題表示を維持したまま、最後の問題であることを知らせる。

### 次にやること

- Week2 Day3-9 に進む。
- 次は、`questions[currentQuestionIndex]` を何度も直接書くのではなく、「現在の問題」を取り出す処理を整理する。
- 目的は、現在表示中の問題データを扱いやすくし、今後の問題追加や処理分割に備えること。

## 2026-06-10

### 完了したこと

- Week2 Day3-9 として、現在の問題データを取り出す処理を `getCurrentQuestion()` 関数にまとめた。
- `getCurrentQuestion()` の中で `questions[currentQuestionIndex]` を返すようにした。
- `renderQuestion(getCurrentQuestion())` の形で、現在の問題を画面に表示するようにした。
- 正誤判定や解説表示でも、現在の問題データを `getCurrentQuestion()` から取得する形に整理した。
- ページ読み込み直後の1問目表示、次の問題ボタンによる2問目表示、正誤判定、解説表示が正しく動くことを確認した。

### 学んだこと

- `getCurrentQuestion()` は、現在の問題データを取り出す関数である。
- データを取り出すだけでは、画面表示は変わらない。
- 画面に表示するには、取り出したデータを `renderQuestion()` に渡す必要がある。
- `renderQuestion(getCurrentQuestion())` は、「現在の問題を取得して、それを画面に表示する」という意味になる。
- 関数の戻り値を、別の関数の引数として使える。

### 詰まった点・注意点

- `getCurrentQuestion();` だけを呼んでも、問題文や選択肢は表示されない。
- `getCurrentQuestion()` は表示担当ではなく、取得担当である。
- 表示担当は `renderQuestion()` なので、初期表示では `renderQuestion(getCurrentQuestion())` が必要。
- 関数を作る時は、「その関数が何を返すのか」「返された値をどこで使うのか」を意識する。

### 次にやること

- Week2 Day3-10 に進む。
- 次は、イベントリスナー内で `getCurrentQuestion()` を何度も呼ぶのではなく、`const currentQuestion = getCurrentQuestion();` のように現在の問題を一度変数に入れて使う。
- 目的は、クリック時処理をさらに読みやすくし、「現在の問題を取得する → その問題で判定する → その問題の解説を表示する」という流れを明確にすること。

## 2026-06-10

### 完了したこと

- Week2 Day3-10 として、回答ボタンのイベントリスナー内で現在の問題データを変数に入れるようにした。
- `const currentQuestion = getCurrentQuestion();` を作成した。
- `isCorrectAnswer(answerText, currentQuestion)` の形で、現在の問題データを使って正誤判定した。
- `showResult` で表示する解説文も `currentQuestion.reasonText` から取得するようにした。
- ブラウザ上の挙動が変わらないことを確認した。

### 学んだこと

- `getCurrentQuestion()` を何度も直接呼ぶ代わりに、変数へ入れてから使うと読みやすくなる。
- `currentQuestion` という変数名により、「今表示中の問題に対して判定している」ことが分かりやすくなる。
- 変数や関数で名前を付けることは、処理の意味を読みやすくするために使える。
- ただし、何でも関数化・変数化すればよいわけではなく、同じ式が何度も出る場合や意味が明確な場合に有効である。

### 詰まった点・注意点

- `currentQuestion` の中身は `getCurrentQuestion()` の戻り値であり、さらにその中身は `questions[currentQuestionIndex]` である。
- 一見すると入れ子が増えて分かりにくく見えるが、コードを「構造」ではなく「意味」で読めるようにする効果がある。
- 今回は「現在の問題」という意味が明確なので、変数化する価値がある。

### 次にやること

- Week2 Day3-11 に進む。
- 次は、「次の問題が存在するかどうか」を判定する処理に名前を付ける。
- 目的は、`currentQuestionIndex < questionCount - 1` のような条件式を、意味で読める形に整理すること。

## 2026-06-10

### 完了したこと

- Week2 Day3-11 として、次の問題があるかどうかを判定する処理を関数にまとめた。
- `hasNextQuestion()` 関数を作成した。
- `currentQuestionIndex < questionCount - 1` の条件を使い、次の問題があるかどうかを判定した。
- 次の問題がある場合は `true`、ない場合は `false` を返すようにした。
- `nextButton` のクリックイベント内で、`if (hasNextQuestion())` を使う形に変更した。
- 1問目から2問目へ進む動作、最後の問題で「最後の問題です」と表示される動作、Consoleにエラーが出ないことを確認した。

### 学んだこと

- 条件式に名前を付けると、コードを意味で読めるようになる。
- `hasNextQuestion()` は「次の問題があるか」を表す関数である。
- `if (hasNextQuestion())` と書くことで、「次の問題があるなら進む」という意図が分かりやすくなる。
- `currentQuestionIndex < questionCount - 1` は、最後の問題より前にいるかどうかを判定している。
- `if / else` で `true` / `false` を返す関数は、比較式をそのまま `return` する形にもできる。

### Week2 Day3 でできるようになったこと

- 複数の問題データを `questions` 配列で管理できるようになった。
- `currentQuestionIndex` で現在の問題番号を管理できるようになった。
- `getCurrentQuestion()` で現在の問題データを取り出せるようになった。
- 「次の問題」ボタンで問題を切り替えられるようになった。
- 最後の問題より先へ進まないように制御できるようになった。
- `hasNextQuestion()` を使い、次の問題があるかどうかを判定できるようになった。
- 表示・正誤判定・解説表示を、現在の問題データに揃えられるようになった。

### 次にやること

- Week2 Day4 に進む。
- 次は、複数問題クイズとしての表示や操作を少し整える。
- まずは「現在何問目か」を画面に表示する、または回答後の操作を整理する方向へ進む。

## 2026-06-10

### 完了したこと

- Week2 Day4-1 として、現在何問目かを画面に表示した。
- HTMLに `id="progress"` を持つ進行度表示エリアを追加した。
- JavaScriptで `progress` 要素を取得した。
- `renderQuestion` の中で、`currentQuestionIndex + 1` と `questions.length` を使って進行度表示を作成した。
- ページ読み込み直後は第1問、次の問題へ進むと第2問と表示が更新されることを確認した。

### 学んだこと

- 配列の添字は0から始まるが、ユーザーに見せる問題番号は1から始める方が自然である。
- 画面表示用には `currentQuestionIndex + 1` を使う。
- `questions.length` を使うと、全問題数を表示できる。
- 問題表示を更新する `renderQuestion` の中で進行度も更新すると、問題文・選択肢・進行度をまとめて更新できる。

### 詰まった点・注意点

- 内部処理用の番号と、画面表示用の番号はずれる。
- `currentQuestionIndex` をそのまま表示すると、第0問のようになってしまう。
- 進行度表示も、問題切り替え時に更新される必要がある。

### 次にやること

- Week2 Day4-2 に進む。
- 次は、進行度表示の処理を `renderQuestion` の中に直接書いたままにするか、別関数に分けるかを検討する。
- 目的は、`renderQuestion` の役割が広がりすぎていないかを考え、必要に応じて表示処理を整理すること。

## 2026-06-10

### 完了したこと

- Week2 Day4-2 として、進行度表示の処理を `renderProgress()` 関数に分けた。
- `renderProgress()` の中で、`currentQuestionIndex + 1` と `questions.length` を使って進行度表示を作成した。
- `progress.textContent` の更新処理を `renderProgress()` に移した。
- `renderQuestion()` の中から `renderProgress()` を呼び出す形にした。
- 問題文・選択肢・進行度表示が、問題切り替え時にまとめて更新されることを確認した。

### 学んだこと

- 画面表示の一部にも、意味のある名前を付けて関数化できる。
- `renderProgress()` のように役割を分けると、コードを読みやすくできる。
- `renderQuestion()` の中から `renderProgress()` を呼ぶことで、問題表示と進行度表示の更新漏れを防げる。
- 内部処理用の `currentQuestionIndex` は0始まりだが、画面表示では `+ 1` して人間向けの番号にする。

### Week2 Day4 でできるようになったこと

- 現在何問目かを画面に表示できるようになった。
- 問題が切り替わった時、進行度表示も一緒に更新できるようになった。
- 進行度表示の処理を `renderProgress()` として関数化できた。
- `renderQuestion()` の役割を整理しながら、関連する表示更新をまとめて扱えるようになった。

### 次にやること

- Week2 Day5 に進む。
- 次は、回答後の操作を整理する。
- 具体的には、回答済みの状態で同じ問題のボタンを何度も押せることをどう扱うか、または回答後に次の問題へ進む流れをどう整えるかを検討する。

## 2026-06-10

### 完了したこと

- Week2 Day5-1 として、回答済みかどうかを表す `isAnswered` を追加した。
- `let isAnswered = false;` として、初期状態では未回答にした。
- 回答ボタンのクリックイベント内で、`if (!isAnswered)` の時だけ正誤判定するようにした。
- 正解・不正解を表示した後、`isAnswered = true;` にして回答済み状態にした。
- 回答済みの問題では、別の選択肢を押しても結果や解説が変わらないことを確認した。
- 次の問題へ進んだ時に `isAnswered = false;` に戻し、次の問題では再び回答できるようにした。

### 学んだこと

- `isAnswered` のような真偽値の変数を使うと、現在の状態を管理できる。
- `if (!isAnswered)` は「まだ回答していないなら」という意味になる。
- 回答後に状態を `true` に変えることで、同じ問題への再回答を防げる。
- 次の問題へ進む時には、回答済み状態を解除する必要がある。
- 状態変数は、画面の動きやイベント処理の可否を制御するために使える。

### 詰まった点・注意点

- 回答済み時に `reason` を「回答済みです」へ書き換えると、解説欄の役割が混ざってしまう。
- 今回は、回答済みなら何もしない方針にした。
- `isAnswered = true;` は、正解・不正解どちらの場合でも必要なので、if/else の外へ出すと読みやすい。

### 次にやること

- Week2 Day5-2 に進む。
- 次は、回答後に選択肢ボタンを押せない見た目・状態にするかどうかを検討する。
- 目的は、内部状態だけでなく、ユーザーにも「回答済み」であることが分かるUIに近づけること。

## 2026-06-10

### 完了したこと

- Week2 Day5-2 として、回答後に選択肢ボタンを押せない状態にした。
- `disableAnswerButtons()` 関数を作成した。
- `enableAnswerButtons()` 関数を作成した。
- 回答後に `isAnswered = true;` とし、`disableAnswerButtons()` を呼ぶようにした。
- 次の問題へ進んだ時に `isAnswered = false;` とし、`enableAnswerButtons()` を呼ぶようにした。
- 一度回答した問題では選択肢ボタンが押せなくなり、次の問題へ進むと再び押せることを確認した。

### 学んだこと

- `disabled = true` にするとボタンは押せなくなる。
- `disabled = false` にするとボタンは再び押せる。
- 内部状態 `isAnswered` と、画面上のボタン状態を同じタイミングで切り替えることで、ユーザー操作を制御できる。
- 複数のボタンに同じ処理をする時は、`answerButtons.forEach` を使える。
- 状態管理は、データ上の状態だけでなく、UIの状態にも反映させる必要がある。

### Week2 Day5 でできるようになったこと

- 回答済みかどうかを `isAnswered` で管理できるようになった。
- 1問につき1回だけ回答を受け付ける形にできた。
- 回答後に別の選択肢を押しても、結果や解説が変わらないようにできた。
- 回答後に選択肢ボタンを押せない状態にできた。
- 次の問題へ進むと、再び回答できる状態へ戻せるようになった。

### 次にやること

- Week2 Day6 に進む。
- 次は、今まで作ってきた複数問題クイズの処理を整理し、関数・状態変数・イベントリスナーの役割を復習する。
- 目的は、Week3のスコア管理に進む前に、現在のコード全体の構造を説明できるようにすること。

## 2026-06-10

### Week2 Day6 理解確認

### 確認したこと

- `questions` は、問題文・選択肢・正解・解説文を持つ問題データを複数入れる配列である。
- `currentQuestionIndex` は、現在表示している問題が `questions` の何番目かを表す。
- `isAnswered` は、現在の問題に対して回答済みかどうかを表す。
- `getCurrentQuestion()` は、現在表示すべき問題データを返す。
- `answerButtons` のクリックイベントでは、未回答の場合だけ、選択肢の文字取得、現在の問題取得、正誤判定、結果表示、回答済み化、ボタン無効化を行う。
- `nextButton` のクリックイベントでは、次の問題がある場合は問題番号を進め、表示を更新し、結果表示を消し、未回答状態に戻し、ボタンを有効化する。
- 次の問題がない場合は、「最後の問題です」と表示する。

### 関数の役割

- `renderQuestion`：問題データを画面に表示する。
- `renderProgress`：現在何問目かを表示する。
- `getCurrentQuestion`：現在の問題データを取得する。
- `hasNextQuestion`：次の問題が残っているかを判定する。
- `isCorrectAnswer`：選んだ答えが正解か判定する。
- `showResult`：正誤と解説文を表示する。
- `clearFeedback`：正誤表示と解説文を空にする。
- `disableAnswerButtons`：選択肢ボタンを押せない状態にする。
- `enableAnswerButtons`：選択肢ボタンを押せる状態に戻す。

### 次にやること

- Week2 Day6-3 として、軽い仕様変更課題を行う。
- 目的は、新しい文法ではなく、どの関数・イベントリスナーを変更すべきか判断する練習をすること。
- Day6-3完了後、Week3 Day1のスコア管理へ進む。

## 2026-06-10

### Week2 Day6-3 完了

### 完了したこと

- 最後の問題で「次の問題」ボタンを押した時の表示を変更した。
- `nextButton` のクリックイベント内にある `else` 側を変更した。
- 以前の「最後の問題です」表示をやめた。
- `showResult("クイズ終了！", "おつかれさまでした。")` を使い、終了メッセージを表示するようにした。
- 1問目から2問目へ進む処理、2問目の回答処理、終了表示が想定通り動くことを確認した。

### 学んだこと

- 仕様変更では、まず「どこを変更すべきか」を見極めることが重要である。
- 今回の変更対象は `nextButton` の `else` 側だけでよかった。
- `renderQuestion`、`renderProgress`、`getCurrentQuestion`、`hasNextQuestion`、`isCorrectAnswer` などは変更不要だった。
- 既存の `showResult` を使えば、result欄とreason欄をまとめて更新できる。

### Week2 完了時点でできるようになったこと

- 複数問題クイズを `questions` 配列で管理できる。
- `currentQuestionIndex` で現在の問題番号を管理できる。
- `getCurrentQuestion()` で現在の問題データを取得できる。
- `hasNextQuestion()` で次の問題があるか判定できる。
- `renderQuestion()` で問題文・選択肢・進行度を表示できる。
- `isCorrectAnswer()` で正誤判定できる。
- `isAnswered` で回答済み状態を管理できる。
- 回答後に選択肢ボタンを無効化し、次の問題で再び有効化できる。
- 最後の問題後にクイズ終了メッセージを表示できる。

### 次にやること

- Week3 Day1 に進む。
- Week3ではスコア管理に入る。
- まずは正解数を表す変数を作り、正解した時だけ加算する処理を学ぶ。

## 2026-06-13

### Week3 Day1-1 完了

### 完了したこと

- Week3 Day1-1 として、正解数を管理する処理を追加した。
- HTMLに `id="quiz-score"` を持つ得点表示エリアを追加した。
- JavaScriptで `quizScore` を `querySelector` により取得した。
- 正解数を保存するための状態変数 `score` を作成し、初期値を `0` にした。
- `renderScore()` 関数を作成し、現在の `score` を得点表示エリアに表示するようにした。
- ページ読み込み直後に `renderScore()` を呼び出し、「現在：0点」と表示されるようにした。
- 回答ボタンのクリック処理内で、正解だった場合だけ `score++` により得点を加算するようにした。
- 回答後に `renderScore()` を呼び出し、画面上の得点表示を現在の `score` に合わせて更新するようにした。
- ブラウザ上で、正解時のみ得点が加算され、不正解時には得点が維持されることを確認した。
- 問題表示、選択肢ボタン、正誤判定、解説表示、次の問題への遷移がこれまで通り動くことを確認した。

### 学んだこと

- `score` は、クイズ全体の正解数を保存するための状態変数である。
- `score` はクリックイベントの中ではなく、関数やイベントリスナーの外側で作ることで、問題をまたいで値を保持できる。
- 正解時だけ加点したい場合は、`if (isCorrect)` の中に `score++` を置く。
- 不正解時の `else` 側には `score++` を書かないことで、不正解では得点が増えないようにできる。
- `renderScore()` は、内部状態である `score` を、画面上の得点表示へ反映するための関数である。
- `score` の値を変えただけでは画面表示は自動で変わらないため、表示を更新する関数を呼ぶ必要がある。
- `renderProgress()` と `renderScore()` はどちらも、現在の状態を画面表示に変換する処理である。

### 詰まった点・注意点

- 得点表示用の文言を関数の外側で定数にすると、最初の `score` の値で固定されてしまう可能性がある。
- 得点表示の文言は、`renderScore()` の中で現在の `score` を使って作る方が安全である。
- `score++` を回答処理の外や、正解・不正解の分岐後に置くと、不正解でも得点が増えてしまう。
- `score` の更新と、得点表示エリアの更新は別の処理として考える必要がある。
- 現在は `score` のみを管理しているが、今後は「回答数」や「全問終了時の結果表示」と区別して考える必要がある。

### 次にやること

- Week3 Day1-2 に進む。
- 次は、正解数と回答数の違いを整理する。
- 必要に応じて、回答数を表す状態変数を追加するか検討する。
- 目的は、「正解数」「回答数」「現在の問題番号」がそれぞれ別の状態であることを理解すること。

## 2026-06-13

### Week3 Day1-2 完了

### 完了したこと

- Week3 Day1-2 として、正解数とは別に回答数を管理する処理を追加した。
- 回答数を保存するための状態変数 `answeredCount` を作成し、初期値を `0` にした。
- `renderScore()` の表示文言を変更し、現在の正解数と回答数をまとめて表示するようにした。
- 得点表示エリアに `現在：○点／回答：○問` の形式で表示できるようにした。
- 回答ボタンのクリック処理内で、正解・不正解の判定後に `answeredCount++` を実行するようにした。
- 正解時には `score++` により正解数が増え、正解・不正解に関係なく `answeredCount++` により回答数が増えることを確認した。
- 回答後に `renderScore()` を呼び出し、画面上の正解数・回答数表示が更新されることを確認した。
- 問題表示、選択肢ボタン、正誤判定、解説表示、次の問題への遷移がこれまで通り動くことを確認した。
- 変更内容を Git に commit した。

### 学んだこと

- `score` は正解数を表す状態変数であり、`answeredCount` は回答数を表す状態変数である。
- 正解数と回答数は似ているが、役割が違うため別々の変数で管理する必要がある。
- 正解数は、`if (isCorrect)` の中で正解だった場合だけ増やす。
- 回答数は、正解・不正解に関係なく、回答が成立した時点で増やす。
- `answeredCount++` を正解時の処理の中に入れると、回答数ではなく正解数と同じ意味になってしまう。
- `renderScore()` は、内部状態である `score` と `answeredCount` を画面表示に反映する関数である。
- 状態変数の値を変えただけでは画面表示は変わらないため、表示更新用の関数を呼ぶ必要がある。

### 詰まった点・注意点

- `answeredCount` は、`isAnswered` と名前が似ているため、役割を混同しないように注意する。
- `isAnswered` は「現在の問題に回答済みかどうか」を表す真偽値である。
- `answeredCount` は「クイズ全体で何問回答したか」を表す数値である。
- `answeredCount++` は、同じ問題で二重に増えないように `if (!isAnswered)` の中で実行する必要がある。
- 現在の構造では、未回答のまま「次の問題」を押すことができるため、次の課題で扱う必要がある。

### 次にやること

- Week3 Day1-3 に進む。
- 次は、未回答のまま「次の問題」へ進める現在の挙動をどう扱うか検討する。
- 小学生向けクイズとして、回答前に次へ進めないようにするか、スキップを許可するかを整理する。
- 目的は、`currentQuestionIndex`、`isAnswered`、`answeredCount` の関係をより明確に理解すること。

## 2026-06-14

### Week3 Day1-3 完了

### 完了したこと

- Week3 Day1-3 として、未回答のまま次の問題へ進めないようにした。
- `nextButton` のクリックイベント内で、最初に `isAnswered` を確認するようにした。
- `isAnswered` が `true` の場合だけ、次の問題へ進む処理またはクイズ終了処理を実行するようにした。
- `isAnswered` が `false` の場合は、`currentQuestionIndex++` を実行せず、問題番号が進まないようにした。
- 未回答のまま「次の問題」ボタンを押した場合、解説欄に「回答してから次に進んでください」と表示するようにした。
- 回答後であれば、これまで通り次の問題へ進めることを確認した。
- 最後の問題に回答した後、「次の問題」ボタンを押すと、これまで通り「クイズ終了！」と表示されることを確認した。
- `score` と `answeredCount` の挙動が壊れていないことを確認した。

### 学んだこと

- `isAnswered` は、現在の問題に回答済みかどうかを表す状態変数である。
- 「次の問題」ボタンでは、次の問題があるかを判定する前に、現在の問題へ回答済みかどうかを確認する必要がある。
- `hasNextQuestion()` は次の問題が存在するかを判定する関数であり、回答済みかどうかは判定していない。
- 未回答の場合は、`currentQuestionIndex++`、`renderQuestion()`、`clearFeedback()`、`enableAnswerButtons()` を実行してはいけない。
- 回答済みの場合だけ、次の問題へ進む処理またはクイズ終了処理を実行する。
- 状態管理では、`currentQuestionIndex`、`isAnswered`、`score`、`answeredCount` がそれぞれ別の役割を持つ。

### 詰まった点・注意点

- `if (!isAnswered)` は「未回答なら」という意味である。
- 未回答の条件の中に次問へ進む処理を入れると、回答していないのに次へ進めてしまう。
- `hasNextQuestion` ではなく、実行するときは `hasNextQuestion()` と括弧付きで呼ぶ必要がある。
- 未回答時の表示は、問題を進めずに注意だけ出す処理にする。
- 今後、結果画面を作る時には、最後の問題回答後の処理と通常の次問処理をさらに整理する必要がある。

### 次にやること

- Week3 Day1-4 に進む。
- 次は、最終問題後の「クイズ終了！」表示を、正解数と回答数を使った結果表示へ発展させる。
- 目的は、蓄積した状態変数 `score` と `answeredCount` を、クイズ終了時の結果表示に利用すること。

## 2026-06-15

### Week3 Day1-4 完了

### 完了したこと

- Week3 Day1-4 として、最終問題に回答した直後に最終成績を表示する処理を追加した。
- 最終問題回答後、ユーザーが「次の問題」ボタンを押さなくても結果が分かるようにした。
- `answerButtons` のクリックイベント内で、回答処理後に `hasNextQuestion()` を使って最終問題かどうかを判定するようにした。
- 最終問題の場合、`quizScore` エリアにクイズ終了メッセージと最終成績を表示するようにした。
- 最終問題の場合、`nextButton.disabled = true;` により「次の問題」ボタンを押せない状態にした。
- `result` と `reason` には、最終問題の正誤と解説を残すようにした。
- 最終成績を `showResult()` で表示すると、最終問題の正誤・解説が上書きされる問題に気づき、成績表示先を `quizScore` エリアに変更した。
- `nextButton` のイベント処理を整理し、未回答時は注意表示を出して処理を止め、回答済みの場合だけ次問へ進む形にした。
- 1問目回答後は次の問題へ進めること、最終問題回答後はすぐに成績表示が出ることを確認した。
- `score`、`answeredCount`、`isAnswered`、`currentQuestionIndex` の挙動が壊れていないことを確認した。

### 学んだこと

- `hasNextQuestion()` は、現在の問題の次に問題があるかを判定する関数である。
- 関数の判定結果を使う場合は、`hasNextQuestion` ではなく `hasNextQuestion()` と括弧付きで呼び出す必要がある。
- `hasNextQuestion` と書いた場合は、関数そのものを参照しているだけであり、判定結果にはならない。
- `currentQuestionIndex++` は「次の問題へ進む」処理なので、回答ボタン側へ移動してはいけない。
- 回答した瞬間に `currentQuestionIndex` を進めると、画面に表示されている問題と内部状態がずれる。
- 最終問題の正誤・解説と、クイズ全体の成績表示は役割が違う。
- `result` と `reason` は現在の問題の正誤・解説を表示する場所として残し、最終成績は `quizScore` に表示する方が自然である。
- UIでは、単に情報を表示できればよいのではなく、ユーザーが確認したい情報を上書きしないことが重要である。

### 詰まった点・注意点

- `if (!hasNextQuestion)` と書くと、関数を実行していないため、最終問題判定が意図通りに動かない。
- 最終問題回答後に `showResult()` で成績を表示すると、最終問題の正誤と解説がすぐに上書きされてしまう。
- 最終問題後に `nextButton` を無効化する場合、今後リスタート機能を追加する時には再び有効化する処理が必要になる。
- `nextButton` の未回答時処理は、安全弁として何となく残すのではなく、「未回答では次へ進めない」という正式な仕様として残す。
- 今後コードが増える場合、最終成績表示は `renderFinalScore()` のような関数に分ける余地がある。

### 次にやること

- Week3 Day1-5 に進む。
- 次は、最終成績表示の処理を関数化するかどうかを検討する。
- 目的は、途中経過表示の `renderScore()` と、最終結果表示の役割を分けて整理することである。

## 2026-06-15

### Week3 Day1-5 完了

### 完了したこと

- Week3 Day1-5 として、最終成績表示の処理を関数に分けた。
- `renderFinalScore()` 関数を作成した。
- これまで `answerButtons` のイベントリスナー内に直接書いていた最終成績表示処理を、`renderFinalScore()` の中へ移した。
- 最終問題に回答した時だけ、`if (!hasNextQuestion())` の中で `renderFinalScore()` を呼ぶようにした。
- 最終問題に回答した後、`nextButton.disabled = true;` により「次の問題」ボタンを押せない状態にした。
- 最終問題の正誤判定と解説表示は、これまで通り `result` と `reason` に残ることを確認した。
- 最終成績は `quizScore` エリアに表示されることを確認した。
- 1問目回答後は通常の途中経過表示、最終問題回答後は最終成績表示になることを確認した。
- 既存の問題表示、正誤判定、解説表示、得点表示、次問遷移が壊れていないことを確認した。

### 学んだこと

- 同じDOM要素を書き換える処理でも、「途中経過表示」と「最終結果表示」は役割が違う。
- `renderScore()` は、クイズ中の現在の得点と回答数を表示する関数である。
- `renderFinalScore()` は、クイズ終了時の最終成績を表示する関数である。
- イベントリスナー内に長い表示文言を直接書くより、意味のある関数へ分けると処理の目的が読みやすくなる。
- 関数化するときは、処理を外に出すだけでなく、その関数を呼ぶ条件も正しく保つ必要がある。
- `renderFinalScore()` は毎回呼ぶのではなく、最終問題に回答した時だけ呼ぶ必要がある。

### 詰まった点・注意点

- `renderFinalScore()` を通常の回答後に毎回呼ぶと、1問目の時点でクイズ終了表示が出てしまう。
- 最終問題かどうかは、`hasNextQuestion()` を使って判定できる。
- 関数名の綴りがずれると別の名前として扱われるため、`renderFinalScore` のように一貫して書く必要がある。
- 現在の `score` は点数というより正解数なので、今後表示文言では「○点」より「○問正解」の方が自然である。

### 次にやること

- Week3 Day1-6 に進む。
- 次は、最終成績の表示文言をより正確にし、必要であれば正答率も表示する。
- 目的は、`score`、`answeredCount`、`questions.length` の違いを使い分け、結果表示に必要な情報を整理することである。

## 2026-06-15

### Week3 Day1-6 完了

### 完了したこと

- Week3 Day1-6 として、最終成績表示の文言を整えた。
- `renderFinalScore()` の表示文言を見直し、`score` を「点」ではなく「正答数」として扱う形に変更した。
- 最終成績表示を「正答数は、全○問中○問」の形にした。
- 全問題数には `questions.length` を使い、正答数には `score` を使うようにした。
- 追加で、正答率を計算して表示する処理を追加した。
- `const correctRate = (score / questions.length) * 100;` により、正答率を計算した。
- 最終問題に回答した後、正答数と正答率が `quizScore` エリアに表示されることを確認した。
- 最終問題の正誤判定と解説表示は、これまで通り `result` と `reason` に残ることを確認した。
- 既存の問題表示、正誤判定、解説表示、得点表示、回答数表示、次問遷移が壊れていないことを確認した。

### 学んだこと

- `score` は点数というより、現在の仕様では正解した問題数を表す状態変数である。
- 変数の意味と画面表示の文言は一致させる必要がある。
- 「全○問中○問正解」のような表示では、全問題数に `questions.length`、正答数に `score` を使う。
- 正答率は、正答数を全問題数で割り、100を掛けることで計算できる。
- `score`、`answeredCount`、`questions.length` は似ているが、それぞれ意味が違う。
- UIに出す日本語は、プログラム上の状態変数の意味とずれないように考える必要がある。

### 詰まった点・注意点

- 「成績」という言葉は、文脈によって「点数」や「評価」を表すことが多く、「正答数」とは少し意味が違う。
- 現在の `score` は「点数」ではなく「正答数」として扱う方が自然である。
- 正答率は、問題数が増えた場合に小数が長く表示される可能性がある。
- 今後は必要に応じて、正答率の小数を丸める処理を追加する。
- 表示文言を整える作業も、単なる文章修正ではなく、状態変数の意味を正しく伝えるための重要な設計である。

### 次にやること

- Week3 Day1-7 に進む。
- 次は、正答率の表示を整える。
- 目的は、小数が長く表示される可能性を防ぎ、ユーザーに読みやすい結果表示へ整えることである。

## 2026-06-15

### Week3 Day1-7 完了

### 完了したこと

- Week3 Day1-7 として、最終成績表示に出している正答率の小数表示を整えた。
- `renderFinalScore()` の中で、正答率の計算結果に `toFixed(1)` を使うようにした。
- 正答率が小数点以下1位まで表示されるようにした。
- 正答率が長い小数にならず、読みやすく表示されることを確認した。
- 最終問題に回答した後、正答数と正答率が正しく表示されることを確認した。
- 最終問題の正誤判定と解説表示が、これまで通り `result` と `reason` に残ることを確認した。
- 既存の問題表示、正誤判定、解説表示、得点表示、回答数表示、次問遷移が壊れていないことを確認した。

### 学んだこと

- 正答率は、`score / questions.length * 100` で計算できる。
- 計算結果をそのまま表示すると、問題数によっては小数が長く表示される可能性がある。
- `toFixed(1)` を使うと、小数点以下1位までの表示に整えられる。
- `toFixed()` は、数値を表示用の文字列に変換する。
- 表示に使うだけなら `toFixed(1)` の結果をそのままテンプレートリテラルに埋め込んでよい。
- 後で数値比較に使う場合は、計算用の数値と表示用の文字列を分けた方が安全である。

### 詰まった点・注意点

- `toFixed(1)` を使うと、`100` ではなく `100.0` のように表示される。
- 小数表示としては整っているが、整数の場合にも `.0` が付くため、今後UIとして気になる場合は別途整える余地がある。
- 現在の正答率は表示用であり、評価分岐やランク判定にはまだ使っていない。
- 今後「80%以上ならよくできました」などの評価を追加する場合は、丸める前の正答率を使う方が安全である。

### Week3 Day1 でできるようになったこと

- `score` で正答数を管理できるようになった。
- `answeredCount` で回答数を管理できるようになった。
- 正答数と回答数を別々の状態として扱えるようになった。
- 未回答のまま次の問題へ進めないように制御できるようになった。
- 最終問題に回答した直後に、最終結果を表示できるようになった。
- 最終問題の正誤判定・解説と、クイズ全体の結果表示を別々の表示エリアに分けられるようになった。
- `renderScore()` と `renderFinalScore()` により、途中経過表示と最終結果表示を関数として分けられるようになった。
- 正答率を計算し、表示用に整形できるようになった。

### 次にやること

- Week3 Day2 に進む。
- Day2では、クイズ終了後の画面状態を整理する。
- まずは「クイズ中」と「クイズ終了後」で、どの要素を表示・非表示・無効化するべきかを整理する。
- 目的は、単に結果を表示するだけでなく、画面全体を状態に応じて制御できるようにすることである。

## 2026-06-16

### Week3 Day2-1 完了

### 完了したこと

- Week3 Day2-1 として、最終問題回答後の「次の問題」ボタンの扱いを変更した。
- 最終問題回答直後に、すぐ最終成績を表示する仕様を見直した。
- 最終問題回答後も、正誤判定と解説文が `result` と `reason` に残るようにした。
- 最終問題回答後に `nextButton.disabled = true;` でボタンを無効化する処理をやめた。
- 最終問題回答後、`nextButton.textContent = "結果を見る";` により、ボタン文言を「結果を見る」に変更した。
- 最終問題以外では、これまで通り「次の問題」ボタンとして機能することを確認した。
- 最終問題回答後、正誤判定と解説文を残したまま、ボタン文言だけが「結果を見る」に変わることを確認した。

### 学んだこと

- 最終問題回答直後に結果画面へ移ると、最後の問題の正誤判定や解説を確認しづらくなる。
- クイズ終了直後の状態と、結果画面を表示する状態は分けて考えられる。
- ボタンの文言を変えることで、同じボタンでも「次の問題へ進む」から「結果を見る」へ役割を変えられる。
- `hasNextQuestion()` を使うと、現在の問題が最終問題かどうかを判定できる。
- UIでは、処理を動かすだけでなく、ユーザーに次に何をすればよいか分かる表示にすることが重要である。

### 詰まった点・注意点

- 案Cでは、最終問題回答直後に `renderFinalScore()` を呼ばない。
- 最終成績は、「結果を見る」ボタンを押した後に表示する方針に変更する。
- 最終問題回答後も、`result` と `reason` は最終問題の正誤判定と解説表示として残す。
- `nextButton.disabled = true;` を残すと、「結果を見る」ボタンを押せなくなるため、今回の仕様には合わない。
- 今後「結果を見る」をクリックした時に、クイズ画面を隠して結果画面を表示する処理が必要になる。

### 次にやること

- Week3 Day2-2 に進む。
- 次は、「結果を見る」ボタンをクリックした時に、結果画面を表示する処理を作る。
- まずは、どの要素を隠し、どの要素を残すかを整理する。
- 目的は、クイズ中の画面と結果画面を状態として分けて扱えるようにすることである。

## 2026-06-16

### Week3 Day2-2 / Day2-3 完了

### 完了したこと

- Week3 Day2-2 として、結果画面で隠す要素と残す要素を整理した。
- 結果画面では、クイズ中の表示である `#progress`、`.question-title`、`#statement`、`.choices`、`#result`、`#reason`、`#next-button` を隠す方針にした。
- 結果画面では、`#quiz-score` だけを残し、最終成績を表示する方針にした。
- クイズ画面部分を隠す関数名として、`hideQuizArea()` を採用した。
- Week3 Day2-3 として、`.question-title` と `.choices` を `querySelector` で取得した。
- `hideQuizArea()` 関数を作成した。
- `hideQuizArea()` の中で、隠したい要素を `hideElements` 配列にまとめた。
- `hideElements.forEach()` を使い、各要素の `style.display` を `"none"` に変更するようにした。
- `nextButton` のクリックイベント内で、次の問題がある場合はこれまで通り次問へ進み、次の問題がない場合は `renderFinalScore()` と `hideQuizArea()` を呼ぶようにした。
- 最終問題回答後に「結果を見る」ボタンをクリックすると、`quizScore` だけが表示され、それ以外のクイズ画面要素が非表示になることを確認した。

### 学んだこと

- 結果画面では、クイズ中に必要だった要素をそのまま残すと、画面の役割が分かりにくくなる。
- クイズ中の画面と結果画面は、表示する要素を分けて考える必要がある。
- `.choices` のような class を `querySelector` で取得する場合は、セレクタとして `".choices"` のようにドット付きで指定する必要がある。
- 複数の要素に同じ処理を行う場合、要素を配列にまとめて `forEach` で処理できる。
- `progress, questionTitle.style.display = "none";` のようにカンマで並べても、複数要素の `display` をまとめて変更することはできない。
- 関数は作っただけでは動かず、必要なタイミングで呼び出す必要がある。
- `hideQuizArea()` は画面を隠す処理、`renderFinalScore()` は最終成績を表示する処理であり、役割が違う。

### 詰まった点・注意点

- `document.querySelector("choices")` と書くと、`<choices>` というHTMLタグを探す意味になるため、今回の `.choices` は取得できない。
- class を取得する場合は、CSSセレクタと同じように `".choices"` と書く。
- 結果画面で `#next-button` を残す場合、そのボタンの役割を別途決める必要がある。
- 今回はまだ「もう一度挑戦」機能を作らないため、結果画面では `#next-button` も隠す方針にした。
- `hideQuizArea()` を呼ぶだけでは最終成績は表示されないため、結果画面へ移る時には `renderFinalScore()` も呼ぶ必要がある。
- 現在はクイズ中も `quizScore` に途中経過が表示されているため、「結果を見る」を押した時の新しい情報が弱いというUI上の課題が残っている。

### 次にやること

- Week3 Day2-4 に進む。
- 次は、クイズ中は途中経過を表示せず、結果画面でだけ最終結果を表示するか検討する。
- 目的は、「結果を見る」ボタンを押した時に、ユーザーにとって新しい結果情報が表示される画面構成にすることである。

## 2026-06-16

### Week3 Day2-4 完了

### 完了したこと

- Week3 Day2-4 として、クイズ中の途中経過表示をやめ、結果画面でだけ最終結果を表示する形に整理した。
- これまでクイズ中に `quizScore` へ途中経過を表示していた `renderScore()` を削除した。
- 初期表示時と回答後に呼んでいた `renderScore()` の呼び出しを削除した。
- `score` の加算処理はそのまま残した。
- `answeredCount` の加算処理も現時点では残した。
- 最終結果の表示は `renderFinalScore()` に一本化した。
- クイズ中は `quizScore` が空のままになり、「結果を見る」ボタンを押した後だけ最終結果が表示されるようにした。
- 最終問題回答後に「結果を見る」ボタンをクリックすると、`hideQuizArea()` によりクイズ画面部分が非表示になり、`quizScore` だけが表示されることを確認した。

### 学んだこと

- 以前は必要だった関数でも、仕様変更後には不要になることがある。
- `renderScore()` は、クイズ中に途中経過を表示するための関数だった。
- 今回の仕様では、クイズ中に途中経過を表示せず、結果画面でだけ最終結果を表示するため、`renderScore()` は不要になった。
- 状態変数である `score` と、画面表示用の関数である `renderScore()` は役割が違う。
- `renderScore()` を削除しても、`score` は `renderFinalScore()` で使うため残す必要がある。
- データを内部で更新することと、それを画面に表示することは別の処理である。
- 結果画面で初めて最終結果を表示することで、「結果を見る」ボタンの意味が明確になる。

### 詰まった点・注意点

- `renderScore()` を削除しても、`score++` を削除してはいけない。
- `score` は最終結果と正答率の計算に必要である。
- `answeredCount` は現時点では表示に使っていないが、回答数を管理する状態変数として残している。
- 今後スキップ機能や回答数表示を入れない場合、`answeredCount` は整理対象になる可能性がある。
- 仕様変更を行った場合は、関数や変数がまだ必要かどうかを見直す必要がある。

### 次にやること

- Week3 Day2-5 に進む。
- 次は、結果画面に残っている `quizScore` の文言や表示内容を確認し、必要であれば結果画面用の見出しや構成を整える。
- 目的は、単に要素を隠すだけでなく、結果画面として分かりやすい表示にすることである。

## 2026-06-16

### Week3 Day2-5 完了

### 完了したこと

- Week3 Day2-5 として、結果画面に表示する文言を整理した。
- `renderFinalScore()` の表示文言を見直した。
- 結果画面の最初に「おつかれさまでした！」と表示するようにした。
- 正答数を「全○問中○問正解！」の形で表示するようにした。
- 正答率を「正答率は○％です！」の形で表示するようにした。
- 全問題数には `questions.length`、正答数には `score`、正答率には `correctRate` を使う形を維持した。
- `toFixed(1)` により、正答率が小数点以下1位まで表示されることを維持した。
- 「結果を見る」ボタンを押した後、クイズ画面部分が非表示になり、`quizScore` に結果文言だけが表示されることを確認した。
- クイズ中は `quizScore` が空のままで、結果画面でだけ結果が表示されることを確認した。

### 学んだこと

- 結果画面では、表示する情報を「終了メッセージ」「正答数」「正答率」に分けて考えると整理しやすい。
- `questions.length` は全問題数、`score` は正答数、`correctRate` は正答率を表す。
- 画面に出す文言は、内部状態の意味と一致させる必要がある。
- `renderFinalScore()` は、結果画面で最終結果を表示するための関数である。
- クイズ中の表示と結果画面の表示を分けることで、「結果を見る」ボタンの意味が明確になる。

### 詰まった点・注意点

- 現在は結果文言を1つの `textContent` にまとめて表示している。
- 今後、結果画面をより見やすくする場合は、見出し・正答数・正答率を別々のHTML要素に分ける余地がある。
- `setTimeout` を使って結果を段階的に表示する案もあるが、今回はまだ実装しない。
- `setTimeout` を入れると、表示順や連打対策など、非同期処理の管理が増える。
- 現時点では、まず結果画面の文言整理を優先した。

### 次にやること

- Week3 Day2-6 に進む。
- 次は、結果画面の表示構造をさらに整えるか、または「もう一度挑戦」ボタンの準備に進む。
- 目的は、結果画面を単なる文言表示から、よりアプリらしい画面状態へ発展させることである。

## 2026-06-17

### Week3 Day2-6 完了

### 完了したこと

- Week3 Day2-6 として、結果画面からもう一度クイズに挑戦できる処理を追加した。
- クイズの状態を初期値に戻すための `resetQuizState()` を作成した。
- `resetQuizState()` の中で、`currentQuestionIndex`、`score`、`answeredCount` を `0` に戻し、`isAnswered` を `false` に戻すようにした。
- 結果画面からクイズを再開するための `retryQuiz()` を作成した。
- `retryQuiz()` の中で `resetQuizState()` を呼び、内部状態を初期化するようにした。
- `retryQuiz()` の中で、結果表示の `quizScore` を空にし、非表示状態も解除するようにした。
- `retryQuiz()` の中で、非表示になっていたクイズ画面要素を再表示するようにした。
- `retryQuiz()` の中で、`nextButton` の文言を「次の問題」に戻すようにした。
- `retryQuiz()` の中で、`clearFeedback()` と `enableAnswerButtons()` を呼び、正誤表示と選択肢ボタンの状態を初期状態に戻すようにした。
- `retryQuiz()` の中で `renderQuestion(getCurrentQuestion())` を呼び、1問目を表示し直すようにした。
- `nextButton` のクリックイベント内で、「もう一度挑戦！」状態を最初に判定し、該当する場合は `retryQuiz()` を呼んで処理を終了するようにした。
- クイズを2周、3周しても、毎回1問目から始まり、得点や回答状態がリセットされることを確認した。
- 再挑戦後も選択肢ボタンが押せる状態になっていることを確認した。

### 学んだこと

- `let currentQuestionIndex = 0;` のような宣言を関数内でもう一度書くと、外側の状態変数ではなく、関数内だけの別変数を作ってしまう。
- 状態を初期化したい場合は、既存の変数に初期値を代入し直す必要がある。
- `resetQuizState()` は内部状態を戻す関数、`retryQuiz()` は画面表示も含めてクイズを再挑戦できる状態に戻す関数である。
- 内部状態の初期化と、画面表示の初期化は役割が違うため、関数を分けると読みやすくなる。
- 同じ `nextButton` でも、画面状態によって「次の問題」「結果を見る」「もう一度挑戦！」と役割が変わる。
- ボタンの役割が変わる場合は、現在の状態を先に判定すると処理を整理しやすい。
- 早期 `return` を使うと、特殊な状態を先に処理し、後続の通常処理へ流れないようにできる。
- `textContent` や `style.display` は大文字・小文字を正しく書く必要がある。

### 詰まった点・注意点

- `textcontent` と書くと、正しい `textContent` とは別物として扱われ、画面上の文字は消えない。
- `style.disply` と書くと、正しい `style.display` とは別物として扱われ、表示状態は変わらない。
- JavaScriptではプロパティ名の大文字・小文字が厳密に区別される。
- 結果画面で非表示にした要素は、再挑戦時に忘れず再表示する必要がある。
- 再挑戦時には、画面表示だけでなく、`score` や `currentQuestionIndex` などの内部状態も戻す必要がある。
- 現在は `nextButton.textContent` を使ってボタンの状態を判定しているが、将来的には `quizMode` のような状態変数で管理する方が堅牢である。

### 次にやること

- Week3 Day2-7 に進む。
- 次は、現在 `nextButton.textContent` に依存している状態判定を見直す。
- 必要であれば、`quizMode` のような状態変数を導入し、「クイズ中」「結果確認前」「結果画面」などの画面状態をより明確に管理する。
- 目的は、UI文言とロジックの結びつきを弱め、状態管理をより安全にすることである。

## 2026-06-17

### Week3 Day2-7 完了

### 完了したこと

- Week3 Day2-7 として、ボタン文言に依存していた状態判定を、状態変数による判定へ変更した。
- 関数やイベントリスナーの外で、画面状態を管理する `quizMode` を作成した。
- `quizMode` の初期値を `"answering"` にした。
- 最終問題に回答したタイミングで、`quizMode` を `"readyToResult"` に変更するようにした。
- 結果画面を表示するタイミングで、`quizMode` を `"result"` に変更するようにした。
- 再挑戦時に、`retryQuiz()` の中で `quizMode` を `"answering"` に戻すようにした。
- `nextButton.textContent === "もう一度挑戦！"` による判定をやめ、`quizMode === "result"` による判定へ変更した。
- ボタンの表示文言は残しつつ、処理の判定には内部状態である `quizMode` を使うようにした。
- ブラウザ上で、これまで通り「次の問題」「結果を見る」「もう一度挑戦！」の流れが動くことを確認した。
- クイズを2周、3周しても、得点・問題番号・回答状態が正しくリセットされることを確認した。

### 学んだこと

- ボタンに表示されている文言と、アプリ内部の状態は別物である。
- `textContent` による判定は、表示文言を変更した時にロジックも壊れる可能性がある。
- `quizMode` のような状態変数を使うと、UI文言に依存せずに処理を分岐できる。
- 同じ `nextButton` でも、画面状態によって「次の問題」「結果を見る」「もう一度挑戦！」という異なる役割を持たせられる。
- 画面状態を変数で管理すると、今どの状態なのかをコード上で明示しやすくなる。
- 早期 `return` と状態変数を組み合わせると、特殊な状態を先に処理し、通常処理と分けて書ける。
- 表示文言はユーザーに見せるためのもの、状態変数はプログラムが判断するためのものとして分けると安全である。

### 詰まった点・注意点

- `quizMode` を追加しただけでは不十分で、実際の条件分岐にも使う必要がある。
- 今回の主目的は、`nextButton.textContent === "もう一度挑戦！"` による判定をやめることだった。
- `quizMode = "readyToResult"` は現時点では状態として持っているが、まだ明示的な分岐には大きく使っていない。
- 今後、状態管理をさらに整理する場合は、`quizMode === "readyToResult"` の分岐を明確に作る余地がある。
- 状態変数が増えると、どのタイミングで値を切り替えるかを丁寧に管理する必要がある。

### 次にやること

- Week3 Day2-8 に進む。
- 次は、`quizMode` を使った分岐をさらに整理し、「answering」「readyToResult」「result」の各状態で何をするかを明確にする。
- 目的は、画面状態と処理内容の対応をより読みやすくし、状態管理の理解を深めることである。

## 2026-06-17

### Week3 Day2-8 完了

### 完了したこと

- Week3 Day2-8 として、`quizMode` ごとに `nextButton` の処理を整理した。
- `quizMode === "result"` の場合は、`retryQuiz()` を呼んで再挑戦するようにした。
- `quizMode === "readyToResult"` の場合は、結果画面を表示する処理へ進むようにした。
- 結果画面を表示する処理を `showResultView()` 関数として切り出した。
- `showResultView()` の中で、`quizMode` を `"result"` に変更し、`renderFinalScore()`、`hideQuizArea()`、`nextButton` の文言変更を行うようにした。
- `quizMode === "readyToResult"` の分岐後に `return` を入れ、結果画面表示後に下の処理へ流れないようにした。
- `quizMode === "answering"` の通常状態では、未回答チェック、次問遷移、最終結果表示へ進む処理を整理した。
- クイズを3周しても、問題番号・得点・回答状態・ボタン文言が正しくリセットされることを確認した。

### 学んだこと

- `quizMode` を使うと、現在の画面状態ごとに処理を分けやすくなる。
- `"answering"`、`"readyToResult"`、`"result"` のように状態を分けることで、同じボタンでも役割を整理できる。
- 結果画面表示のように複数箇所から呼ぶ可能性がある処理は、`showResultView()` のような関数にまとめると重複を減らせる。
- 分岐の中で処理が完了した場合は、`return` によって後続処理へ流れないようにする必要がある。
- ブラウザ上で動作が変わらないように見えても、内部で同じ処理が複数回呼ばれている可能性がある。
- 状態ごとの処理を上から順に読めるようにすると、コードの見通しがよくなる。

### 詰まった点・注意点

- 関数名の綴りは、動作だけでなく可読性にも影響する。
- `showResultView()` のような関数名は、処理内容を正しく表す名前にする必要がある。
- `quizMode === "readyToResult"` の分岐で `showResultView()` を呼んだ後に `return` しないと、下の処理へ進んでしまう。
- 現在は `quizMode` の値を文字列で直接書いているため、将来的には綴り間違いを防ぐ工夫が必要になる可能性がある。

### 次にやること

- Week3 Day2-9 に進む。
- 次は、Day2全体の仕上げとして、現在の関数と状態変数の役割を整理する。
- 目的は、Day3に進む前に、クイズ中・結果表示前・結果画面・再挑戦の流れをコード上で確認しやすくすることである。

## 2026-06-17

### Week3 Day2-9 完了

### 完了したこと

- Week3 Day2-9 として、Day2で作成・整理した状態変数と関数を棚卸しした。
- 現在の状態変数として、`currentQuestionIndex`、`isAnswered`、`score`、`quizMode` を確認した。
- 現在の関数として、問題表示、正誤表示、ボタン制御、結果画面、初期化・再挑戦に関する関数を確認した。
- 現行仕様では `answeredCount` がほぼ使われていないことを確認した。
- 未回答のまま次へ進めない仕様では、回答数は基本的に全問題数と一致するため、`answeredCount` を整理対象として扱った。
- Day2終了時点の動作確認チェックリストを実施した。
- ページ読み込み、未回答時の注意表示、正誤判定、解説表示、次問遷移、最終問題後の結果表示、再挑戦まで、全項目が想定通り動くことを確認した。
- 2周、3周しても、問題番号・得点・回答状態・ボタン文言が正しくリセットされることを確認した。

### 学んだこと

- 仕様変更後には、以前必要だった状態変数が不要になることがある。
- 状態変数は、現在の仕様で実際に使われているかを確認する必要がある。
- `score` は最終結果に必要だが、`answeredCount` は現行仕様では必須ではない。
- 関数を役割ごとに整理すると、コード全体の構造を把握しやすくなる。
- 動作確認では、1回だけでなく、再挑戦を含めて2周・3周確認することが重要である。
- 画面状態と内部状態の両方が正しくリセットされているかを見る必要がある。

### 詰まった点・注意点

- 使われていない変数を残すと、後からコードを読む時に意味を誤解する可能性がある。
- ただし、将来スキップ機能や回答数表示を追加する場合には、回答数を管理する状態変数が再び必要になる可能性がある。
- 状態変数を削除する時は、VSCodeなどで全検索し、参照が残っていないか確認する必要がある。
- 再挑戦機能では、表示だけでなく、`currentQuestionIndex`、`score`、`isAnswered`、`quizMode` などの内部状態も戻す必要がある。

### Week3 Day2 でできるようになったこと

- クイズ中画面と結果画面を分けられるようになった。
- 最終問題回答後に「結果を見る」ボタンを表示できるようになった。
- 結果画面ではクイズ中の要素を隠し、最終結果だけを表示できるようになった。
- 結果画面から「もう一度挑戦！」でクイズを再開できるようになった。
- `resetQuizState()` と `retryQuiz()` により、内部状態と画面状態を初期化できるようになった。
- `quizMode` により、ボタン文言ではなく内部状態で処理を分岐できるようになった。
- `showResultView()` により、結果画面表示処理を関数としてまとめられるようになった。
- 状態変数と関数を棚卸しし、不要な状態を整理できるようになった。

### 次にやること

- Week3 Day3-1 に進む。
- 次は、`quizMode` の値を文字列のまま直接書いている現在の状態を見直す。
- 目的は、`"answering"`、`"readyToResult"`、`"result"` のような状態名の綴り間違いを防ぎ、状態管理をさらに安全にすることである。

## 2026-06-17

### Week3 Day3-1 完了

### 完了したこと

- Week3 Day3-1 として、`quizMode` に使っている状態名を定数化した。
- コード冒頭に `QUIZ_MODE` 定数オブジェクトを作成した。
- `QUIZ_MODE` の中に、`ANSWERING`、`READY_TO_RESULT`、`RESULT` を定義した。
- `let quizMode = "answering";` を `let quizMode = QUIZ_MODE.ANSWERING;` に置き換えた。
- `quizMode = "answering";`、`quizMode = "readyToResult";`、`quizMode = "result";` のように直接文字列を書いていた箇所を、`QUIZ_MODE` 経由の記述に置き換えた。
- `quizMode === "result"` のような判定を、`quizMode === QUIZ_MODE.RESULT` のような判定に置き換えた。
- VSCodeの検索で、`"answering"`、`"readyToResult"`、`"result"` の置き換え漏れがないことを確認した。
- ブラウザ上でクイズを3周し、問題表示、正誤判定、結果表示、再挑戦の動作に問題がないことを確認した。

### 学んだこと

- 状態名をコード中に直接何度も書くと、綴り間違いによって条件分岐が壊れる可能性がある。
- `QUIZ_MODE` のような定数オブジェクトに状態名をまとめると、状態の種類を一か所で管理できる。
- `QUIZ_MODE.ANSWERING` のように意味のある名前を経由して値を使うと、コードを読んだ時に状態の意味が分かりやすくなる。
- 定数化の目的は、値を短くすることではなく、意味のある値を一か所に集めて管理しやすくすることである。
- JavaScriptは上から順に実行されるため、`QUIZ_MODE` を使う前に定義しておく必要がある。
- 置き換え作業の後は、検索で漏れを確認することが重要である。

### 詰まった点・注意点

- `QUIZ_MODE` はオブジェクトであり、`QUIZ_MODE.ANSWERING` のようにプロパティを通して値を取り出す。
- `QUIZ_MODE` を導入しても、プロパティ名を間違えた場合に必ずエラーになるとは限らない。
- ただし、文字列を毎回手打ちするより、補完が効きやすくなり、状態名の管理もしやすくなる。
- 状態名を定数化した後も、ブラウザ上で複数回動作確認する必要がある。
- 今後状態の種類が増えた場合は、まず `QUIZ_MODE` に追加してから使うようにする。

### 次にやること

- Week3 Day3-2 に進む。
- 次は、現在の `quizMode` を使った処理をさらに読みやすくするため、ボタン文言の変更処理を整理する。
- 目的は、状態そのものと、状態に応じた画面表示を分けて考えられるようにすることである。

## 2026-06-17

### Week3 Day3-2 完了

### 完了したこと

- Week3 Day3-2 として、`nextButton` の文言更新処理を関数にまとめた。
- `updateNextButtonText()` 関数を作成した。
- `updateNextButtonText()` の中で、現在の `quizMode` に応じて `nextButton.textContent` を切り替えるようにした。
- `quizMode === QUIZ_MODE.ANSWERING` の場合は、ボタン文言を「次の問題」にするようにした。
- `quizMode === QUIZ_MODE.READY_TO_RESULT` の場合は、ボタン文言を「結果を見る」にするようにした。
- `quizMode === QUIZ_MODE.RESULT` の場合は、ボタン文言を「もう一度挑戦！」にするようにした。
- `quizMode` を変更している箇所を検索し、状態変更後に `updateNextButtonText()` を呼ぶ形へ整理した。
- これまで直接 `nextButton.textContent = ...` と書いていた処理を、できる範囲で `updateNextButtonText()` の呼び出しへ置き換えた。
- ブラウザ上でクイズを3周し、「次の問題」「結果を見る」「もう一度挑戦！」の文言切り替えと動作に問題がないことを確認した。

### 学んだこと

- ボタン文言は、現在の `quizMode` に応じて決められる。
- 状態を変更する処理と、状態に応じて表示を更新する処理は分けて考えられる。
- `quizMode` を変更した後に `updateNextButtonText()` を呼ぶことで、内部状態と画面表示を対応させられる。
- `nextButton.textContent` を複数箇所で直接変更するより、専用の関数にまとめた方が変更に強くなる。
- `if` を複数並べても動くが、状態が1つだけに決まる場合は `else if` を使うと意図が読みやすくなる。
- 文字列や文言を変更したい時、変更箇所が1つの関数にまとまっていると修正しやすい。

### 詰まった点・注意点

- `updateNextButtonText()` は `quizMode` を変更する関数ではない。
- `updateNextButtonText()` は、すでに変更された `quizMode` を見て、ボタン表示を更新する関数である。
- 順番としては、先に `quizMode` を変更し、その後で `updateNextButtonText()` を呼ぶ必要がある。
- `nextButton.textContent` の直接代入が残っていないか、検索で確認することが重要である。
- ボタン文言の変更処理を関数にまとめることで、状態管理と表示更新の分離が進んだ。

### 次にやること

- Week3 Day3-3 に進む。
- 次は、現在の画面表示更新処理をさらに整理し、`quizMode` に応じた画面状態の切り替えを見直す。
- 目的は、ボタン文言だけでなく、画面全体を状態に応じて更新する考え方へ進むことである。

## 2026-06-18

### Week3 Day3-3 完了

### 完了したこと

- Week3 Day3-3 として、`quizMode` に応じて画面表示を切り替える処理を整理した。
- 結果画面で隠す対象となる要素を `hideElements` 配列にまとめた。
- `progress`、`questionTitle`、`statement`、`choices`、`result`、`reason` を、結果画面では非表示にする対象として整理した。
- `nextButton` は `ANSWERING`、`READY_TO_RESULT`、`RESULT` のどの状態でも表示するため、非表示対象から外した。
- `updateScreenByQuizMode()` 関数を作成した。
- `quizMode === QUIZ_MODE.RESULT` の場合は、クイズ画面部分を隠し、`quizScore` を表示するようにした。
- `quizMode` が `RESULT` 以外の場合は、クイズ画面部分を表示し、`quizScore` を非表示にするようにした。
- 以前の `hideQuizArea()` と役割が重複していたため、結果画面表示処理を `updateScreenByQuizMode()` に寄せる形へ整理した。
- `showResultView()` の中で、結果文言の表示、ボタン文言の更新、画面表示状態の更新を行うようにした。
- ブラウザ上でクイズを3周し、問題表示、正誤判定、結果表示、再挑戦の動作に問題がないことを確認した。

### 学んだこと

- `quizMode` は、ボタン文言だけでなく、画面全体の表示状態にも使える。
- 状態が変わったら、状態に応じて画面表示も更新する必要がある。
- 表示・非表示を切り替える要素は、配列にまとめて `forEach` で処理できる。
- `ANSWERING` と `READY_TO_RESULT` は、画面表示としてはほぼ同じであり、主な違いはボタン文言と正誤・解説が残っている点である。
- `RESULT` の時だけ、問題文・選択肢・正誤・解説を隠し、結果表示を見せる。
- `hideQuizArea()` のような一方向の関数より、`updateScreenByQuizMode()` のように状態に応じて画面を整える関数の方が、状態管理と相性がよい。

### 詰まった点・注意点

- `nextButton` は結果画面でも「もう一度挑戦！」として使うため、非表示対象に含めない。
- 関数名の綴りは、動作だけでなく可読性にも影響するため、`showResultView()` のように正しい名前へ統一する必要がある。
- `quizMode` を変更しただけでは画面表示は自動では変わらない。
- 状態を変えた後に、ボタン文言や画面表示を更新する処理を呼ぶ必要がある。
- 現時点では、`quizMode` の変更、ボタン文言更新、画面表示更新が複数箇所に分かれているため、次の課題でさらに整理する余地がある。

### 次にやること

- Week3 Day3-4 に進む。
- 次は、`quizMode` の変更と、それに伴う表示更新処理をまとめる。
- 目的は、`quizMode = ...`、`updateNextButtonText()`、`updateScreenByQuizMode()` を毎回別々に書かなくてもよい形に近づけることである。

## 2026-06-18

### Week3 Day3-4 完了

### 完了したこと

- Week3 Day3-4 として、`quizMode` の変更と、それに伴う表示更新処理をまとめた。
- `setQuizMode(newMode)` 関数を作成した。
- `setQuizMode(newMode)` の中で、`quizMode` に `newMode` を代入するようにした。
- `setQuizMode(newMode)` の中で、`updateNextButtonText()` と `updateScreenByQuizMode()` を呼ぶようにした。
- これまで `quizMode` を直接変更していた箇所を、`setQuizMode(QUIZ_MODE.xxx)` の呼び出しへ置き換えた。
- `showResultView()` では、`setQuizMode(QUIZ_MODE.RESULT)` と `renderFinalScore()` を呼ぶ形に整理した。
- `retryQuiz()` では、再挑戦時に `setQuizMode(QUIZ_MODE.ANSWERING)` を呼ぶ形に整理した。
- 最終問題回答後は、`setQuizMode(QUIZ_MODE.READY_TO_RESULT)` により、「結果を見る」状態へ切り替えるようにした。
- `showReusltView` という誤った綴りが残っていないか検索し、0件であることを確認した。
- ブラウザ上でクイズを3周し、問題表示、正誤判定、結果表示、再挑戦の動作に問題がないことを確認した。

### 学んだこと

- 状態を変更する処理と、その状態に応じた表示更新処理はセットで扱う必要がある。
- `setQuizMode()` を作ることで、`quizMode` を変更する時にボタン文言と画面表示も同時に更新できる。
- `updateNextButtonText()` と `updateScreenByQuizMode()` は役割が違うため分けたままにし、`setQuizMode()` の中でまとめて呼ぶ形が自然である。
- 細かい責任は関数ごとに分けつつ、セットで必要な処理は上位関数で束ねると、呼び忘れを防ぎやすい。
- `showResultView()` は、結果画面へ移る一連の操作を表す関数として残す意味がある。
- `===` は比較、`=` は代入であり、状態変更には代入を使う必要がある。

### 詰まった点・注意点

- `quizMode === newMode;` と書くと、比較しているだけで `quizMode` は変更されない。
- `quizMode = newMode;` と書くことで、初めて `quizMode` の値が変更される。
- 関数名の綴りが間違っていても、定義名と呼び出し名が一致していればJavaScript上は動くことがある。
- ただし、人間が読む時の意味が崩れるため、`showResultView()` のように正しい名前へ統一する必要がある。
- ブラウザ画面上で動いているように見えても、通常ルートでは通らない箇所に未定義関数や誤綴りが残っている可能性がある。
- 修正後は、画面確認だけでなく検索確認も行うことが重要である。

### 次にやること

- Week3 Day3-5 に進む。
- 次は、`quizMode` の状態遷移を棚卸しする。
- 目的は、どの操作で `ANSWERING`、`READY_TO_RESULT`、`RESULT` のどの状態へ移るのかを整理し、状態管理が破綻していないか確認することである。

## 2026-06-18

### Week3 Day3-5 完了

### 完了したこと

- Week3 Day3-5 として、`quizMode` の状態遷移を棚卸しした。
- ページ読み込み直後の `quizMode` は `QUIZ_MODE.ANSWERING` であることを確認した。
- 通常問題に回答した時は、`quizMode` が変わらず `QUIZ_MODE.ANSWERING` のままであることを確認した。
- 最終問題に回答した時は、`quizMode` が `QUIZ_MODE.READY_TO_RESULT` に変わることを確認した。
- 「結果を見る」ボタンを押した時は、`quizMode` が `QUIZ_MODE.RESULT` に変わることを確認した。
- 「もう一度挑戦！」ボタンを押した時は、`quizMode` が `QUIZ_MODE.ANSWERING` に戻ることを確認した。
- `nextButton` の文言と、`nextButton` をクリックした時の処理を分けて整理した。
- `ANSWERING` の時は、未回答チェックまたは次問遷移を行う状態であると整理した。
- `READY_TO_RESULT` の時は、結果画面を表示する状態であると整理した。
- `RESULT` の時は、クイズをリセットして1問目から再挑戦する状態であると整理した。

### 学んだこと

- `quizMode` は、現在のクイズがどの状態にあるかを表す状態変数である。
- 状態遷移を整理すると、どの操作でどの状態へ移るのかが分かりやすくなる。
- `nextButton` の文言を変えることと、`nextButton` をクリックした時の処理は別の役割である。
- `updateNextButtonText()` は、`quizMode` に応じてボタン文言を更新する関数である。
- `nextButton` のイベントリスナーは、`quizMode` に応じてクリック時の処理を分ける場所である。
- 同じボタンでも、状態によって「次の問題」「結果を見る」「もう一度挑戦！」という異なる役割を持たせられる。
- 状態ごとの役割を言葉で整理しておくと、コードの分岐を読みやすくできる。

### 詰まった点・注意点

- 「nextButton に何を表示するか」と「nextButton を押した時に何をするか」を混同しないようにする。
- `ANSWERING` の時でも、未回答の場合と回答済みの場合で処理が分かれる。
- `READY_TO_RESULT` は、最終問題に回答済みだが、まだ結果画面には移っていない状態である。
- `RESULT` は、結果画面が表示されており、次のクリックで再挑戦する状態である。
- 状態遷移を整理しておくことで、今後機能を追加した時にどこへ処理を入れるべきか判断しやすくなる。

### 次にやること

- Week3 Day3-6 に進む。
- 次は、Day3全体の仕上げとして、現在の関数と状態管理の最終確認を行う。
- 目的は、不要な処理や重複が残っていないか確認し、Week3 Day3を完了できる状態にすることである。

## 2026-06-18

### Week3 Day3-6 完了

### 完了したこと

- Week3 Day3-6 として、Day3で整理した状態管理の最終確認を行った。
- `quizMode =` を検索し、`quizMode` の直接代入が不要に残っていないか確認した。
- `nextButton.textContent =` を検索し、ボタン文言の直接代入が `updateNextButtonText()` に集約されていることを確認した。
- `style.display` を検索し、画面表示切り替え処理が `updateScreenByQuizMode()` に集約されていることを確認した。
- 以前使っていた `hideQuizArea()` が残っていないことを確認した。
- `if (quizMode = QUIZ_MODE.RESULT)` のように、比較ではなく代入になっていた箇所を修正した。
- `if` 文の条件では、状態を変更するのではなく、`===` で現在の状態を比較する必要があることを確認した。
- 修正後、クイズを3周して、問題表示、正誤判定、結果表示、再挑戦の挙動に問題がないことを確認した。

### 学んだこと

- `=` は代入、`===` は比較である。
- `if` の条件式で `quizMode = QUIZ_MODE.RESULT` と書くと、判定ではなく代入になってしまう。
- 状態を調べたい時は、`quizMode === QUIZ_MODE.RESULT` のように比較する必要がある。
- 検索による棚卸しは、不要な直接代入や表示更新の散らばりを見つけるのに有効である。
- 状態変更は `setQuizMode()`、ボタン文言更新は `updateNextButtonText()`、画面表示更新は `updateScreenByQuizMode()` という役割分担で整理できる。
- ブラウザ上で動いているように見えても、条件式の書き間違いによって内部状態が意図せず変わっている可能性がある。
- 動作確認とコード検索の両方を行うことで、表面上の動作だけでは見えない問題を見つけやすくなる。

### Week3 Day3 でできるようになったこと

- `QUIZ_MODE` によって状態名を定数化できるようになった。
- `quizMode` を使って、クイズ中・結果表示前・結果画面を区別できるようになった。
- `updateNextButtonText()` により、状態に応じてボタン文言を更新できるようになった。
- `updateScreenByQuizMode()` により、状態に応じて画面表示を切り替えられるようになった。
- `setQuizMode()` により、状態変更と表示更新をセットで扱えるようになった。
- `showResultView()` により、結果画面へ移る一連の処理を関数としてまとめられるようになった。
- 状態遷移を言葉で整理し、各状態で `nextButton` が何をするべきか説明できるようになった。

### 次にやること

- Week3を完了とし、次はWeek4 Day1に進む。
- Week4では、`fetch`、JSON、エラー処理の基礎に入る。
- まずは、現在JavaScript内に直接書いている `questions` データを、外部データとして扱う準備を行う。
- 目的は、問題データと画面処理を分け、将来的にAPIやJSONファイルから問題を読み込める構造へ近づけることである。

## 2026-06-19

### Week4 Day1-1〜Day1-8 完了

### 完了したこと

- Week4 Day1として、クイズの問題データを外部JSONファイルから読み込む準備と実装を行った。
- 現在の `questions` 配列に含まれる情報を棚卸しした。
- 1問あたりのデータとして、`statement`、`correctAnswer`、`reasonText`、`choices` が必要であることを確認した。
- `statement` は問題文表示、`choices` は選択肢表示、`correctAnswer` は正誤判定、`reasonText` は解説表示に使うことを確認した。
- JavaScriptオブジェクトとJSONの違いを確認した。
- JSONではキー名を `"statement"` のようにダブルクォーテーションで囲む必要があることを確認した。
- JSONファイルには `const questions =` のような変数宣言やセミコロンを書かないことを確認した。
- `questions.json` を `index.html` と同じ階層に作成した。
- `questions.json` に問題データを移した。
- JSONでは、問題データ全体を配列として持ち、各問題をオブジェクトとして並べる形にした。
- `script.js` 側の `questions` を、外部JSONから後で代入するために `let questions = [];` の形に変更した。
- `loadQuestionsData()` 関数を作成した。
- `loadQuestionsData()` の中で `fetch("questions.json")` を使い、`questions.json` を読み込むようにした。
- `response.json()` を使って、Responseの本文をJavaScriptで扱える配列に変換するようにした。
- 読み込んだ配列を、外側の `questions` に代入するようにした。
- 問題データを読み込んだ後に、`renderQuestion(getCurrentQuestion())` を呼んで1問目を表示するようにした。
- これまで最初に直接呼んでいた `renderQuestion(getCurrentQuestion())` を、`loadQuestionsData()` の呼び出しに置き換えた。
- `hasNextQuestion()` が、読み込み前に固定された `questionCount` ではなく、現在の `questions.length` を見る形に修正した。
- 修正後、クイズを3周して、問題表示、正誤判定、次問遷移、結果表示、再挑戦が正しく動作することを確認した。

### 学んだこと

- JSONはJavaScriptに似ているが、JavaScriptコードそのものではなく、データだけを書く形式である。
- JSONでは、キー名と文字列をダブルクォーテーションで囲む必要がある。
- JSONでは、変数宣言、関数、コメント、セミコロン、末尾の余計なカンマを書けない。
- `fetch()` は外部ファイルを取りに行く処理であり、取得結果として最初に返るのは問題データそのものではなくResponseオブジェクトである。
- `response.json()` は、Responseの本文をJSONとして読み取り、JavaScriptで使える配列やオブジェクトに変換する処理である。
- `fetch` と `response.json()` は完了を待つ必要があるため、`async` と `await` を使う。
- 外部JSONから読み込む場合、`questions` は最初から中身を持つ配列ではなく、最初は空の配列として用意し、読み込み後に代入する。
- 関数の中で `let questions = ...` と書くと、外側の `questions` とは別のローカル変数を作ってしまう。
- 外側の `questions` に読み込んだデータを入れたい場合は、`questions = await response.json();` のように代入する必要がある。
- `renderQuestion(getCurrentQuestion())` 自体の考え方は、外部JSON化しても大きく変わらない。
- 変わるのは、`questions` が最初から存在するのではなく、読み込み完了後に使えるようになる点である。
- `questions.length` のように後から変わる値を、読み込み前に `const questionCount = questions.length;` として固定してしまうと、古い値のままになってしまう。
- 外部データを使う場合は、現在のデータ状態をその都度見る処理にする必要がある。

### 詰まった点・注意点

- `let questions = await response.json();` と書いたため、関数内だけの別の `questions` が作られ、外側の `questions` が空のままになった。
- 外側の `questions` が空のまま `renderQuestion(getCurrentQuestion())` を呼ぶと、`getCurrentQuestion()` が `undefined` を返し、`question.statement` を読もうとしてエラーになった。
- `questions.json` の読み込み自体は成功していても、読み込んだデータを正しい変数に代入できていなければ、画面表示には使えない。
- `const questionCount = questions.length;` を読み込み前に書くと、`questionCount` が `0` のまま固定され、1問目の時点で次の問題がないと判定されてしまった。
- `hasNextQuestion()` では、固定された `questionCount` ではなく、現在の `questions.length` を使う必要がある。
- `fetch("questions.json")` は、`index.html` をファイルとして直接開いている場合に失敗することがあるため、必要に応じてLive Serverなどで `http://...` として開く必要がある。
- 一時確認用の `console.log(questions);` は、確認が終わったら削除する。

### 次にやること

- Week4 Day1-9 に進む。
- 次は、`questions.json` が見つからない場合やJSONの形式が壊れている場合に備え、読み込み失敗時のエラー処理を整理する。
- 目的は、読み込みに失敗した時に画面が無反応になるのではなく、ユーザーに分かる形でエラーメッセージを表示できるようにすることである。

## 2026-06-21

### Week4 Day1-9 完了

### 完了したこと

- Week4 Day1-9 として、`questions.json` の読み込み失敗時に画面へエラーメッセージを表示する処理を追加した。
- `loadQuestionsData()` の中で、`fetch("questions.json")` の結果として受け取った `response` に対して `response.ok` を確認するようにした。
- `response.ok` が `false` の場合、`statement.textContent` に「データ読み込みに失敗しました」と表示するようにした。
- 読み込み失敗時は `return;` により、`response.json()` や `renderQuestion(getCurrentQuestion())` に進まないようにした。
- 一時的に `fetch("questions.json")` のファイル名を間違え、読み込み失敗時の表示を確認した。
- ファイル名を元に戻し、通常時はクイズが正しく動作することを確認した。
- 修正後、コミットを行った。

### 学んだこと

- `response.ok` は、HTTPステータスコードが成功扱いかどうかを判定する値である。
- `questions.json` が見つからない場合など、`response.ok` が `false` になるケースがある。
- 読み込みに失敗した時は、画面にエラーメッセージを表示するだけでなく、その後の処理を止める必要がある。
- `return;` を使うことで、失敗時に `response.json()` や `renderQuestion(getCurrentQuestion())` へ進むことを防げる。
- エラー時に処理を止めないと、問題データが存在しないまま表示処理や正誤判定が動き、別のエラーにつながる可能性がある。
- 正常系だけでなく、あえて失敗状態を作って確認することで、エラー処理が本当に動いているか確かめられる。

### 詰まった点・注意点

- `response.ok` が `false` の時にメッセージを表示しても、`return;` がなければ後続処理が実行されてしまう。
- `response.ok` で拾えるのは、主にファイルが見つからない場合やサーバー側のエラーである。
- JSONファイルの中身が壊れている場合など、`response.ok` だけでは拾えない失敗もある。
- 失敗確認のためにファイル名を変更した場合は、確認後に必ず元の `questions.json` に戻す必要がある。

### 次にやること

- Week4 Day1-10 に進む。
- 次は、`response.ok` では拾えない失敗に備え、`try...catch` を使ったエラー処理を学ぶ。
- 目的は、JSONの形式が壊れている場合や、`response.json()` の処理で失敗した場合にも、画面へ分かりやすいエラーメッセージを表示できるようにすることである。

## 2026-06-21

### Week4 Day1-11 完了

### 完了したこと

- Week4 Day1-11 として、問題データ読み込み中の表示と操作制御を追加した。
- `loadQuestionsData()` の読み込み開始時に、`statement.textContent` へ「問題を読み込んでいます……」と表示するようにした。
- 読み込み開始時に `disableAnswerButtons()` を呼び、問題データの読み込みが完了するまで回答ボタンを押せないようにした。
- `fetch("questions.json")` による読み込みが成功した後、`questions = await response.json();` で問題データを `questions` に代入する流れを維持した。
- 問題データを読み込んだ後、`renderQuestion(getCurrentQuestion())` で1問目を表示するようにした。
- 1問目の表示後に `enableAnswerButtons()` を呼び、回答ボタンを押せる状態に戻すようにした。
- 一時的に `setTimeout` を使って読み込み処理を遅らせ、「問題を読み込んでいます……」の表示が実際に確認できることを確かめた。
- 確認後、テスト用の遅延処理は削除する方針とした。

### 学んだこと

- `fetch` が速い場合、読み込み中メッセージは一瞬しか表示されないことがある。
- ローカルの軽いJSONファイルでは読み込みが非常に速いため、読み込み中表示が見えにくいのは自然である。
- 読み込み中表示の目的は、必ず長時間表示することではなく、「まだ問題データを使える状態ではない」と画面上で示すことである。
- 読み込み中は、問題データが存在しない可能性があるため、回答ボタンを無効化しておくのが安全である。
- 読み込み成功後に問題を表示し、その後で回答ボタンを有効化することで、表示前に回答されることを防げる。
- 確認用の `setTimeout` は、動作確認には有効だが、本番用の処理として残すべきではない。

### 詰まった点・注意点

- 読み込み中メッセージは、処理が速いとほとんど見えない。
- 読み込み中表示を確認したい場合、一時的に `setTimeout` を使って遅延を入れる方法がある。
- ただし、確認用の遅延処理を残すと、不要にユーザーを待たせることになるため、確認後は削除する必要がある。
- 読み込み失敗時には、回答ボタンを有効化しない方が安全である。
- 問題データがない状態で回答ボタンを押せると、未定義の問題データを参照してエラーになる可能性がある。

### 次にやること

- Week4 Day1-12 に進む。
- 次は、外部JSON読み込み後の構成を棚卸しし、`script.js` 側に残すべき処理と `questions.json` 側に置くべきデータが分離できているか確認する。
- 目的は、Day1で行った外部JSON化・読み込み・エラー処理・読み込み中状態を整理し、不要な処理や確認用コードが残っていないか確認することである。

## 2026-06-21

### Week4 Day1-12 完了

### 完了したこと

- Week4 Day1-12 として、外部JSON読み込み後の構成を棚卸しした。
- `script.js` に古い `questions` 配列の問題データ本体が残っていないことを確認した。
- `questions` が、外部JSONから読み込んだ配列を後から代入できるように `let questions = [];` の形になっていることを確認した。
- `questions.json` が配列形式になっており、各問題データがオブジェクトとして並んでいることを確認した。
- `loadQuestionsData()` の中で、読み込み中表示、回答ボタン無効化、`fetch`、`response.ok` 確認、`response.json()`、`questions` への代入、1問目表示、回答ボタン有効化の順番になっていることを確認した。
- 一時確認用の `setTimeout` や `console.log` が残っていないことを確認した。
- `hasNextQuestion()` が、固定された `questionCount` ではなく、現在の `questions.length` を見て判定していることを確認した。
- ページ初期表示用として `renderQuestion(getCurrentQuestion())` を直接呼び出している箇所が残っていないことを確認した。
- 初期処理として `loadQuestionsData()` が呼ばれていることを確認した。
- 通常状態でクイズを3周し、問題表示、正誤判定、次問遷移、結果表示、再挑戦が正しく動くことを確認した。

### 学んだこと

- 外部JSON化後は、ページ読み込み直後に直接 `renderQuestion(getCurrentQuestion())` を呼ぶのではなく、`loadQuestionsData()` による読み込み完了後に呼ぶ必要がある。
- `renderQuestion(getCurrentQuestion())` 自体が不要になるわけではなく、呼び出すタイミングと場所が重要である。
- `loadQuestionsData()` 内の `renderQuestion(getCurrentQuestion())` は、JSON読み込み完了後に1問目を表示するために必要である。
- `retryQuiz()` 内の `renderQuestion(getCurrentQuestion())` は、再挑戦時に1問目を表示し直すために必要である。
- `nextButton` のイベントリスナー内の `renderQuestion(getCurrentQuestion())` は、次の問題へ進む時に必要である。
- 初期表示・再挑戦・次問遷移では、同じ `renderQuestion(getCurrentQuestion())` を使っていても、それぞれ役割が異なる。
- `questions.json` は問題データを持ち、`script.js` は読み込み、表示、正誤判定、状態管理などの処理を持つ、という分担に整理できた。

### 詰まった点・注意点

- 「最初に直接 `renderQuestion(getCurrentQuestion())` を呼ばない」という確認は、すべての `renderQuestion(getCurrentQuestion())` を削除するという意味ではない。
- 初期表示前は `questions` がまだ空の可能性があるため、直接呼び出すと危険である。
- 一方、JSON読み込み後、再挑戦時、次問遷移時には、現在の問題を表示するために `renderQuestion(getCurrentQuestion())` が必要である。
- 外部JSON化後も、既存の表示関数や正誤判定関数の考え方は大きく変わらない。
- 変わったのは、問題データが「最初から存在する」のではなく、「読み込み完了後に使える」ようになった点である。

### Week4 Day1 でできるようになったこと

- JavaScript内に直接書いていた問題データを、`questions.json` へ分離できるようになった。
- JSONの基本的な書き方を確認できた。
- `fetch()` を使って外部JSONファイルを読み込めるようになった。
- `response.json()` を使って、Responseの本文をJavaScriptの配列として扱える形に変換できるようになった。
- 読み込んだ問題データを `questions` に代入し、既存の `renderQuestion()` や `isCorrectAnswer()` と組み合わせて使えるようになった。
- `response.ok` を使って、ファイルが見つからない場合などの読み込み失敗を扱えるようになった。
- `try...catch` を使って、JSONの形式不正など `response.ok` だけでは拾えないエラーを扱えるようになった。
- 読み込み中メッセージと回答ボタン無効化により、データ読み込み中にユーザーが操作できない状態を作れるようになった。
- 外部データ読み込み後も、次問遷移、結果表示、再挑戦の既存機能を維持できるようになった。

### 次にやること

- Week4 Day2 に進む。
- 次は、外部JSONから読み込む問題データの件数を増やし、データ追加時に `script.js` を変更しなくてもクイズ内容を増やせることを確認する。
- 目的は、問題データとアプリ処理の分離をさらに実感し、JSON側のデータ追加だけでアプリが拡張できる状態を確認することである。

## 2026-06-21

### Week4 Day2-1 完了

### 完了したこと

- Week4 Day2-1 として、`questions.json` に3問目の問題データを追加した。
- 3問目にも、`statement`、`correctAnswer`、`reasonText`、`choices` のキーを用意した。
- 2問目と3問目の間にカンマを入れ、JSON配列として正しい形にした。
- 3問目の最後には余計なカンマを付けないようにした。
- `script.js` は一切変更せず、`questions.json` の変更だけで問題数を増やした。
- ブラウザ上で3問目が出題されることを確認した。
- 3問構成でも、問題表示、正誤判定、次問遷移、結果表示、再挑戦が正しく動作することを確認した。

### 学んだこと

- 問題データを `questions.json` に分離しておくと、問題を増やす時に `script.js` を変更しなくてよい。
- `script.js` 側が `questions.length` を使っていれば、JSON側の問題数が増えても、次問判定や結果画面の総問題数が自動的に変わる。
- `hasNextQuestion()` が現在の `questions.length` を見ているため、2問構成から3問構成に変わっても正しく次問判定できる。
- 結果画面の「全○問中×問正解」も `questions.length` を使っているため、問題数の変更に対応できる。
- 外部JSON化の利点は、データ追加とアプリ処理の変更を分けられることにある。

### 詰まった点・注意点

- JSONでは、問題オブジェクト同士の間にはカンマが必要である。
- ただし、最後の問題オブジェクトの後ろには余計なカンマを付けない。
- `statement`、`correctAnswer`、`reasonText`、`choices` のキーが揃っていないと、表示・判定・解説の処理で問題が起きる可能性がある。
- JSONの書式としては、キー名と文字列をダブルクォーテーションで囲む必要がある。
- 動作上は問題なくても、インデントやスペースを整えると、後からデータを追加・確認しやすくなる。

### 次にやること

- Week4 Day2-2 に進む。
- 次は、JSONデータの形が壊れている場合を想定し、1問ごとのデータに必要なキーが揃っているかを確認する考え方に入る。
- 目的は、外部データを読み込む時に「読み込めたが中身の形がおかしい」ケースへ対応する準備をすることである。

## 2026-06-22

### Week4 Day2-4 完了

### 完了したこと

- `isValidQuestion(question)` を作成し、1問分の問題データが正しい形か判定できるようにした。
- `statement`、`correctAnswer`、`reasonText` が空でない文字列か確認する処理を追加した。
- `choices` が配列であること、選択肢ボタン数と同じ要素数であること、各要素が空でない文字列であることを確認する処理を追加した。
- `correctAnswer` が `choices` の中に含まれていることを確認する処理を追加した。
- `loadQuestionsData()` の中で、読み込んだデータを `loadedQuestions` に一度入れ、検証後に `questions` へ代入する形にした。
- 読み込んだデータが配列でない場合、空配列の場合、1問でも形式不正がある場合は、エラーメッセージを表示して処理を止めるようにした。
- `questions.json` を一時的に壊し、空配列、`choices` 欠落、`choices` 数不足、`correctAnswer` 不一致の各ケースで想定通りエラーメッセージが出ることを確認した。
- テスト後、`questions.json` を正常な3問構成に戻し、通常通り3周できることを確認した。

### 学んだこと

- `return` は関数の処理をその場で終了するため、検証失敗時には後続処理へ進ませない目的で使える。
- 外部データは、JSONとして読み込めるだけでは十分ではなく、アプリが期待する形かどうかを検証する必要がある。
- `every()` を使うと、配列内の全要素が条件を満たすか確認できる。
- `questions.every(isValidQuestion)` によって、全ての問題データが正しい形かまとめて判定できる。
- `question.choices.every(...)` によって、全ての選択肢が正しい形か判定できる。
- 検証前のデータを直接 `questions` に入れず、`loadedQuestions` として仮置きしてから検証する方が安全である。

### 詰まった点・注意点

- 検証は `renderQuestion()` の後ではなく、表示処理に進む前に行う必要がある。
- 不正データを検証する前に `renderQuestion()` に渡すと、`undefined` に対して `forEach` などを実行してエラーになる可能性がある。
- `every()` は空配列に対して `true` を返すため、問題数が0件でないことは別途確認する必要がある。
- `isValidQuestion()` は画面表示を担当せず、`true` / `false` を返す判定専門の関数にした方が役割が分かりやすい。

### 次にやること

- Week4 Day2-5 に進む。
- 次は、データ検証処理をさらに見通しよくするために、共通して使う「空でない文字列かどうか」の判定を小さな関数に切り出す。

## 2026-06-22

### Week4 Day2-5 完了

### 完了したこと

- `isNonEmptyString(value)` を作成し、空でない文字列かどうかを判定できるようにした。
- `isValidQuestion(question)` 内で重複していた文字列判定を、`isNonEmptyString()` に置き換えた。
- `statement`、`correctAnswer`、`reasonText` の判定を `isNonEmptyString()` で行うようにした。
- `choices` の各要素が空でない文字列かどうかを、`question.choices.every(isNonEmptyString)` で確認するようにした。
- 正常な3問構成で、クイズを3周できることを確認した。
- `questions.json` の `statement` や `choices` の中身を一時的に空文字にし、想定通りエラーメッセージが表示されることを確認した。
- テスト後、`questions.json` を正常な3問構成に戻した。

### 学んだこと

- 同じ判定処理が複数箇所に出てくる場合は、小さな関数に切り出すと読みやすくなる。
- `isNonEmptyString()` のように関数名で意味を表すことで、`isValidQuestion()` の中身が理解しやすくなる。
- `return false` 版の早期 return を使うと、どの条件で不正と判断しているかを順番に読みやすい。
- `every()` には、既存の関数をそのまま渡すことができる。
- `question.choices.every(isNonEmptyString)` と書くことで、全ての選択肢が空でない文字列かどうかをまとめて判定できる。

### 詰まった点・注意点

- `return` を忘れると、正常な場合に `undefined` が返ってしまう。
- `isNonEmptyString()` を短く書く場合でも、true と false の意味を逆にしないよう注意する。
- 判定処理を関数化しても、実際に正常系・異常系の両方で動作確認する必要がある。

### 次にやること

- Week4 Day2-6 に進む。
- 次は、データ検証エラー時の表示や処理を整理し、読み込み失敗・JSON文法エラー・データ形式エラーの違いをより見通しよく扱う。

## 2026-06-22

### Week4 Day2-6 完了

### 完了したこと

- 読み込み時のエラーメッセージ表示処理を `showLoadError(message)` に切り出した。
- `loadQuestionsData()` 内で直接 `statement.textContent` にエラー文を代入していた箇所を、`showLoadError(...)` に置き換えた。
- `return` は `showLoadError()` の中に入れず、`loadQuestionsData()` 側に残した。
- 正常な3問構成でクイズが正しく動作することを確認した。
- ファイル名ミスによる読み込み失敗時に、読み込み失敗メッセージが表示されることを確認した。
- JSON文法エラーやデータ形式エラー時に、エラーメッセージが表示され、クイズ開始に進まないことを確認した。
- テスト後、`questions.json` を正常な3問構成に戻した。

### 学んだこと

- 関数には、できるだけ一つの目的を持たせると役割が分かりやすくなる。
- `showLoadError(message)` は「エラー文を画面に表示する」ことだけを担当する。
- `return` は `loadQuestionsData()` の後続処理を止めるためのものなので、呼び出し元に残す必要がある。
- 小関数化しても、処理を止める責任と画面表示の責任は分けて考える必要がある。

### 詰まった点・注意点

- `showLoadError()` の中で `return` しても、終了するのは `showLoadError()` だけであり、`loadQuestionsData()` は止まらない。
- 外部データの検証では、配列かどうかを確認する前に `.every()` や `.includes()` を呼ばないよう順番に注意する。
- エラー文言は、読み込み失敗・JSON文法エラー・データ形式エラーで意味が異なるため、どのケースでどの表示になるか確認しておく必要がある。

### 次にやること

- Week4 Day2-7 に進む。
- 次は、Week4 Day2で追加したデータ検証とエラー処理を棚卸しし、外部JSON読み込みまわりの最終確認を行う。

## 2026-06-22

### Week4 Day3-3 完了

### 完了したこと

- 読み込んだデータ全体が配列でない場合に、「読み込んだデータが配列になっていません。」と表示するようにした。
- 読み込んだ配列が空の場合に、「読み込んだデータに問題が入っていません。」と表示するようにした。
- `loadedQuestions.every(isValidQuestion)` が `false` になった場合、`forEach` を使って不正な問題の番号を Console に表示するようにした。
- `index + 1` を使い、配列上の添字ではなく、人間に分かりやすい「○問目」として表示できるようにした。
- JSON全体が配列でない場合、空配列の場合、2問目のデータを壊した場合で、それぞれ想定通りの表示・Console出力になることを確認した。
- テスト後、`questions.json` を正常な3問構成に戻した。

### 学んだこと

- `every()` は全体の正誤判定には使いやすいが、どの要素が不正かまでは分からない。
- `forEach(function (question, index) { ... })` を使うと、各問題データとその位置を扱える。
- 配列の `index` は0から始まるため、人間向けの「○問目」にするには `index + 1` を使う。
- 画面には利用者向けの簡潔なエラーを出し、Consoleには開発者向けの詳しい情報を出すと、混乱を避けつつ修正しやすくなる。

### 詰まった点・注意点

- テンプレートリテラルでは、`${index + 1}` のように書く。
- Console用メッセージにも余計な記号が入らないよう注意する。
- テストのために `questions.json` を壊した後は、必ず正常な形に戻してからコミットする。

### 次にやること

- Week4 Day3-4 に進む。
- 次は、Consoleに「何問目」だけでなく、「どの項目が不正なのか」まで出せる構成を考える。

## 2026-06-22

### Week4 Day3-5 完了

### 完了したこと

- `getQuestionValidationErrors(question)` を作成し、1問分の問題データに含まれる不備をエラー文の配列として返せるようにした。
- `isValidQuestion(question)` を、`getQuestionValidationErrors(question).length === 0` を返す形に整理した。
- `isValidQuestion()` は有効かどうかの判定だけを担当し、詳細なエラー内容の判定は `getQuestionValidationErrors()` に任せる形にした。
- `loadQuestionsData()` 内の `forEach` で `getQuestionValidationErrors(question)` を呼び、エラーがある問題について Console に問題番号と詳細メッセージを表示するようにした。
- 正常な3問構成では Console にエラーが出ず、クイズを3周できることを確認した。
- 2問目の `statement` を一時的に空文字にし、Console に想定通りの詳細エラーが出ることを確認した。
- 2問目の `correctAnswer` を `choices` に存在しない値にし、Console に想定通りの詳細エラーが出ることを確認した。
- テスト後、`questions.json` を正常な3問構成に戻した。

### 学んだこと

- `true / false` だけを返す関数では、どの項目が不正なのかまでは分からない。
- エラー文の配列を返す関数を作ると、1問分のデータに複数の不備があってもまとめて確認できる。
- `isValidQuestion()` は、詳細な検証結果の配列が空かどうかを見るだけにすると簡潔になる。
- Console には、利用者向け画面よりも詳しい開発者向け情報を出すと、データ修正がしやすくなる。
- `!` の有無ひとつで、正常データを不正扱いしてしまうことがあるため、条件式の向きに注意する必要がある。

### 詰まった点・注意点

- `getQuestionValidationErrors()` の条件式で `!` が抜けると、正常な問題データにもエラーが出てしまう。
- `choices` が配列かどうかを確認する前に、`choices.length` や `choices.includes()` を使わないよう順番に注意する。
- Console に配列をそのまま出すより、`forEach` で1行ずつエラーメッセージを表示した方が読みやすい。

### 次にやること

- Week4 Day3-6 に進む。
- 次は、詳細なデータ検証処理を最終確認し、問題数を増やしても管理しやすい構成になっているか棚卸しする。

## 2026-06-22

### Week4 Day3-6 完了

### 完了したこと

- Week4 Day3で追加した詳細エラー表示まわりを棚卸しした。
- `getQuestionValidationErrors(question)` が、1問分の問題データを受け取り、エラー文の配列を返す関数であることを確認した。
- `getQuestionValidationErrors(question)` が空配列を返す場合は、その1問分の問題データが正しい形式であると整理した。
- `isValidQuestion(question)` は、`getQuestionValidationErrors(question).length === 0` によって、1問分の問題データが有効かどうかを判定する関数であると確認した。
- `loadedQuestions.every(isValidQuestion)` は、読み込んだ全問題が正しい形式かどうかを確認する処理であると整理した。
- `forEach(function (question, index) { ... })` は、不正な問題が何問目かを Console に表示するために使っていると確認した。
- 画面表示は利用者向け、Console表示は開発者向けとして役割を分ける理由を整理した。
- `choices` が配列かどうかを確認する前に、`length`、`every()`、`includes()` を使うと危険であることを確認した。

### 学んだこと

- `true / false` の判定と、詳細なエラー診断は分けて考えると見通しがよくなる。
- `getQuestionValidationErrors()` は「どこが悪いか」を診断する関数であり、`isValidQuestion()` は「有効かどうか」だけを判断する関数である。
- `isValidQuestion(question)` は1問分の判定を行い、`loadedQuestions.every(isValidQuestion)` は全問題の判定を行う。
- 利用者には簡潔なエラー文を表示し、開発者には Console で問題番号と詳細エラーを表示すると、混乱を避けつつ修正しやすくなる。
- 問題数が10問、20問と増えても、Console に「何問目のどの項目が悪いか」が出れば、データ不備を見つけやすい。

### 詰まった点・注意点

- `isValidQuestion(question)` と `loadedQuestions.every(isValidQuestion)` では、見ている範囲が違う。
- `choices` が配列か確認する前に、`choices.length`、`choices.every()`、`choices.includes()` を呼ぶと、意味の違う判定になったり、エラーで止まったりする可能性がある。
- Console に出す情報は開発者向けなので詳しくてよいが、画面に出す情報は利用者向けに簡潔にする。

### 次にやること

- Week4 Day4 に進む。
- 次は、外部JSON化とデータ検証を前提に、問題追加・保守・表示まわりをさらに扱いやすくする。

## 2026-06-22

### Week4 Day4-2 完了

### 完了したこと

- `questions.json` の各問題データに `id` を追加した。
- 既存の3問に `science-001`、`science-002`、`science-003` のような識別用IDを付けた。
- `id` は画面表示用ではなく、開発・管理用のデータとして追加した。
- `script.js` は変更せず、既存のクイズ動作に影響がないことを確認した。
- ブラウザ上で3問が通常通り表示され、回答・結果表示・再挑戦が正しく動作することを確認した。
- Console に不要なエラーが出ていないことを確認した。

### 学んだこと

- JSONに新しいキーを追加しても、そのキーを `script.js` 側で使っていなければ既存の表示処理には影響しない。
- `index + 1` は現在の並び順を表すが、`id` は問題データ自体を識別するための値である。
- 問題データに `id` を持たせると、問題の並び順が変わっても特定の問題を追跡しやすくなる。

### 次にやること

- Week4 Day4-3 に進む。
- 次は、追加した `id` を検証対象に加え、`id` が空または文字列でない場合にエラーとして検出できるようにする。

## 2026-06-22

### Week4 Day4-3 完了

### 完了したこと

- `questions.json` に追加した `id` を、問題データの検証対象に加えた。
- `getQuestionValidationErrors(question)` の中で、`question.id` が空でない文字列かどうかを確認するようにした。
- `id` が空、または文字列でない場合は、エラー配列にメッセージを追加するようにした。
- 正常な3問構成では、既存のクイズ動作に影響がないことを確認した。
- 1問分の `id` を一時的に空文字にし、Console に詳細エラーが表示されることを確認した。
- 画面には利用者向けに「読み込んだデータに問題が発見されました。」が表示されることを確認した。
- テスト後、`questions.json` を正常な形に戻し、通常通り動作することを確認した。

### 学んだこと

- `id` は画面表示用ではなく、問題データを管理・識別するための値である。
- `id` も他の必須項目と同じように、外部データとして検証対象に含める必要がある。
- `isNonEmptyString()` を使えば、`id` が空でない文字列かどうかを既存の共通判定で確認できる。
- 問題データに管理用のキーを追加しても、表示処理で使っていなければ既存のクイズ動作には影響しない。

### 詰まった点・注意点

- `question` がオブジェクトであることを確認した後に、`question.id` を確認する。
- `id` の検証は、問題文や選択肢の検証より前に置くと、管理用識別子の不備を早めに確認しやすい。
- エラーメッセージでは、JSONのキー名である `id` と表記を揃えると、修正対象が分かりやすい。

### 次にやること

- Week4 Day4-4 に進む。
- 次は、Console に表示するエラー情報へ `id` を活用し、何問目だけでなく、どの問題IDに不備があるかも分かるようにする。

## 2026-06-22

### Week4 Day4-4 完了

### 完了したこと

- Console に表示するデータ検証エラーで、問題番号だけでなく `id` を使えるようにした。
- `question.id` が空でない文字列である場合は、Console に `science-002` のような問題IDを表示するようにした。
- `question.id` が空、または問題データ自体が不正な場合は、代わりに `index + 1` を使って「2問目」のように表示するようにした。
- 正常な3問構成では Console エラーが出ないことを確認した。
- 2問目の `correctAnswer` を一時的に不正な値にし、Console に `science-002` が表示されることを確認した。
- 2問目の `id` を一時的に空文字にし、Console に `2問目` が表示されることを確認した。
- テスト後、`questions.json` を正常な3問構成に戻した。

### 学んだこと

- `id` を使うと、問題の並び順に依存せず、どの問題に不備があるかを追跡しやすくなる。
- ただし、`id` 自体が壊れている可能性もあるため、`index + 1` による「○問目」表示も残しておくと安全である。
- 条件演算子 `条件 ? true の時の値 : false の時の値` を使うと、条件に応じて代入する値を切り替えられる。
- `question.id` を読む前に、`question` 自体がオブジェクトであり `null` ではないことを確認すると安全である。

### 詰まった点・注意点

- `id` が正常なら `id` を表示し、`id` が不正なら「○問目」を表示する、という退避先を用意しておく。
- `question` が `null` などの場合、いきなり `question.id` を読もうとするとエラーになる。
- Console は開発者向けなので、修正対象を特定しやすい情報を出すとよい。

### 次にやること

- Week4 Day4-5 に進む。
- 次は、`id` の重複チェックを考える。
- 問題IDは管理用の識別子なので、同じ `id` が複数の問題に使われていないか確認できるようにする。

## 2026-06-22

### Week4 Day4-6 / Day4-7 完了

### 完了したこと

- 問題IDの重複チェックについて設計した。
- `id` が空でない文字列かどうかは1問ごとの検証で確認し、`id` が他の問題と重複していないかは問題一覧全体の検証で確認する、と整理した。
- `findDuplicateQuestionIds(questions)` を作成し、読み込んだ問題データ全体から重複している `id` を探せるようにした。
- `checkedIds` にこれまで見たIDを保存し、同じIDが再度出てきた場合に `duplicateIds` に追加する流れを作った。
- `isNonEmptyString(question.id)` を満たすIDだけを重複チェック対象にするようにした。
- `loadQuestionsData()` の中で、1問ごとの形式チェックが通った後、`questions` に代入する前に重複IDチェックを行うようにした。
- 重複IDが見つかった場合、Console に重複しているIDを表示し、画面には「読み込んだデータに問題が発見されました。」を表示して処理を止めるようにした。
- `questions.json` のIDを一時的に重複させ、Console に重複IDが表示されることを確認した。
- テスト後、`questions.json` を正常な3問構成に戻した。

### 学んだこと

- 1問単体で判定できる検証と、問題一覧全体を見ないと判定できない検証がある。
- `id` が空でない文字列かどうかは1問単体で判定できるが、`id` が重複していないかどうかは全問題を見ないと判定できない。
- 重複チェックでは、これまでに見たIDを保存する配列と、重複していたIDを保存する配列を分けると考えやすい。
- `includes()` を使うと、あるIDがすでに配列内に存在するかを確認できる。
- 重複IDのような開発者向け情報は Console に表示し、利用者向け画面には簡潔なエラー文を表示するとよい。

### 詰まった点・注意点

- `checkedIds` や `duplicateIds` は `forEach` の外で作る必要がある。
- `forEach` の中で配列を作ると、1問ごとに空の配列が作り直され、前に見たIDを記憶できない。
- `id` が空文字や文字列でない場合は、重複チェックではなく、1問ごとの形式エラーとして扱う方が整理しやすい。
- 重複チェックは、各問題の形式チェックが通った後、`questions` に代入する前に行うと安全である。

### 次にやること

- Week4 Day4-8 に進む。
- 次は、Day4で追加した `id` 管理と重複チェックの棚卸しを行い、問題データ管理まわりを最終確認する。

## 2026-06-22

### Week4 Day4-8 完了

### 完了したこと

- Week4 Day4で追加した `id` 管理と重複チェックについて棚卸しした。
- 問題データに `id` を持たせる目的を、問題の並び順に依存せず識別しやすくするためだと整理した。
- `index + 1` による「○問目」は現在の並び順に依存し、`question.id` は問題データ自体に紐づく識別子であると確認した。
- `id` が空でない文字列かどうかは1問分のデータだけで判定できるが、`id` が重複しているかどうかは問題一覧全体を見ないと判定できないと整理した。
- `getQuestionValidationErrors(question)` では1問ごとの形式チェックを行い、`findDuplicateQuestionIds(questions)` では問題一覧全体の重複チェックを行うと整理した。
- `checkedIds` はこれまでに出現したIDを記録するため、`duplicateIds` は重複していたIDを記録するために使うと確認した。
- `id` の重複チェックは、1問ごとの形式チェックが終わった後に行うのが安全だと確認した。
- Console表示では、`id` が正常なら `id` を使い、`id` が不正なら `index + 1` による「○問目」表示へ退避する理由を整理した。

### 学んだこと

- `id` は、問題の順番が変わっても特定の問題を追跡しやすくするための管理用識別子である。
- 問題数が増えた場合、`id` があれば検索などで該当問題に辿り着きやすくなる。
- `id` の形式チェックと重複チェックは別物であり、前者は1問単体、後者は問題一覧全体を対象にする。
- 重複チェックでは、これまで見たIDを保存する配列と、重複していたIDを保存する配列を分けると考えやすい。
- 利用者向け画面には簡潔なエラーを表示し、開発者向けConsoleには修正対象が分かる詳細情報を表示するとよい。
- `id` 管理は問題特定に役立ち、重複チェックはIDの一意性を担保するために役立つ。

### 詰まった点・注意点

- `checkedIds` は「確認済み」というより、「これまでに出現したID」を記録する配列である。
- `id` が空文字や文字列でない場合は、重複チェックより前に1問ごとの形式エラーとして扱う。
- `id` 自体が壊れている場合に備えて、Console表示では `index + 1` による退避先を残しておくと安全である。
- 重複IDがあった場合は、`questions` に代入する前に処理を止める必要がある。

### 次にやること

- Week4 Day5 に進む。
- 次は、外部JSON化・データ検証・ID管理まで含めた問題データ運用を前提に、画面表示や操作まわりをさらに整理する。

## 2026-06-22

### Week4 Day5-2 完了

### 完了したこと

- 読み込み中や読み込みエラー時に `nextButton` を非表示にする処理を追加した。
- `hideNextButton()` と `showNextButton()` を用意し、次へボタンの表示・非表示を切り替えられるようにした。
- `loadQuestionsData()` の読み込み開始時に `hideNextButton()` を呼び、正常に問題を表示できた後に `showNextButton()` を呼ぶようにした。
- `questions.json` のファイル名を一時的に誤らせた場合、`nextButton` が表示されないことを確認した。
- `questions.json` を一時的に空配列にした場合、`nextButton` が表示されないことを確認した。
- テスト後、`questions.json` を正常形に戻し、通常通りクイズが動作することを確認した。

### 学んだこと

- 読み込み中やエラー時には、利用者が押せてはいけないボタンを画面から隠すと混乱を防げる。
- `nextButton` は通常出題中には必要だが、読み込み失敗時やデータ形式エラー時には不要である。
- エラー時の表示文だけでなく、画面上の操作可能な部品も安全な状態にする必要がある。
- 正常に問題データを読み込めた後だけ、`nextButton` を再表示する流れにすると安全である。

### 次にやること

- Week4 Day5-3 に進む。
- 次は、読み込み中やエラー時に、選択肢ボタン側の見え方も整理する。

## 2026-06-22

### Week4 Day5-3 完了

### 完了したこと

- 読み込み中や読み込みエラー時に、選択肢エリアを非表示にする処理を追加した。
- `.choices` 全体を取得し、`hideChoicesArea()` と `showChoicesArea()` で表示・非表示を切り替えられるようにした。
- `loadQuestionsData()` の読み込み開始時に選択肢エリアを隠し、正常に問題を表示できた後に再表示するようにした。
- 正常な3問構成では、読み込み後に選択肢ボタンが表示され、通常通りクイズを進められることを確認した。
- `questions.json` のファイル名ミスや空配列の異常系で、選択肢エリアが表示されないことを確認した。
- テスト後、`questions.json` を正常な形に戻した。

### 学んだこと

- 読み込み中やエラー時には、押せないボタンを disabled にするだけでなく、選択肢エリアごと非表示にすると利用者に分かりやすい。
- `hideChoicesArea()` / `showChoicesArea()` は、選択肢エリアを見せるかどうかを担当する。
- `disableAnswerButtons()` / `enableAnswerButtons()` は、表示されている選択肢ボタンを押せるかどうかを担当する。
- 選択肢エリアを隠す処理と、回答ボタンを無効化する処理は似ているが、役割は異なる。
- 回答後の二重回答防止には、引き続き `disableAnswerButtons()` が必要である。

### 詰まった点・注意点

- 読み込み中・エラー時だけを見ると `disableAnswerButtons()` は重複気味に見えるが、回答後の状態管理ではまだ必要である。
- 表示・非表示の制御と、ボタンの有効・無効の制御を混同しないようにする。
- エラー時には、問題データが使える状態になっていないため、選択肢エリアも次へボタンも表示しない方がよい。

### 次にやること

- Week4 Day5-4 に進む。
- 次は、読み込み中・エラー時に問題タイトルや進行度表示をどう扱うか整理する。

## 2026-06-25

### Week4 Day5-4 完了

### 完了したこと

- `QUIZ_MODE` に `LOADING` と `LOAD_ERROR` を追加した。
- 初期状態の `quizMode` を `QUIZ_MODE.LOADING` にした。
- 読み込み失敗、空配列、問題形式エラーなど、クイズを開始できない場合に `QUIZ_MODE.LOAD_ERROR` へ切り替えるようにした。
- 正常に問題データを読み込み、1問目を描画できた後に `QUIZ_MODE.ANSWERING` へ切り替えるようにした。
- `updateScreenByQuizMode()` に、読み込み中・読み込みエラー・結果画面それぞれの表示制御を追加した。
- 読み込み中・読み込みエラー時には、問題タイトル・進行度・選択肢・結果表示・解説表示・次へボタンを非表示にし、`statement` のエラーメッセージだけを表示するようにした。
- 結果画面では、問題文などのクイズ本体を非表示にし、結果表示と「もう一度挑戦」ボタンを表示するようにした。
- 通常クイズを3周できることを確認した。
- `questions.json` のファイル名ミス、空配列、問題形式エラーで、エラーメッセージだけが表示され、選択肢ボタンや次へボタンが表示されないことを確認した。
- テスト後、`questions.json` を正常形に戻した。

### 学んだこと

- 読み込み中と読み込みエラーは、通常出題中や結果画面とは別の画面状態として扱うと整理しやすい。
- `LOADING` は読み込み途中の状態、`LOAD_ERROR` はクイズを開始できない状態として分けると意味が明確になる。
- エラー時には、アプリの目的が分かるメッセージは残しつつ、問題データがある前提の表示は隠す方が自然である。
- 結果画面では `nextButton` が「もう一度挑戦」ボタンとして必要になるため、非表示にしないよう注意する。
- 画面状態ごとに表示・非表示を `updateScreenByQuizMode()` に集約すると、個別に `style.display` を操作するより見通しがよくなる。

### 次にやること

- Week4 Day5-5 に進む。
- 次は、読み込みエラー時に何度も出てくる `setQuizMode(QUIZ_MODE.LOAD_ERROR)` と `showLoadError(...)` の扱いを整理する。

## 2026-06-25

### Week4 Day5-5 完了

### 完了したこと

- 読み込みエラー時に繰り返し書いていた `setQuizMode(QUIZ_MODE.LOAD_ERROR)` と `showLoadError(...)` を、`showErrorScreen(errorDetail)` に切り出した。
- `showErrorScreen(errorDetail)` の中で、画面モードを `LOAD_ERROR` に切り替えたうえで、`statement` にエラーメッセージを表示するようにした。
- `loadQuestionsData()` 内の各エラー箇所を `showErrorScreen(...)` に置き換えた。
- 各エラー箇所の `return` は、呼び出し元である `loadQuestionsData()` 側に残した。
- `questions.json` のファイル名ミス、空配列、問題形式不正、id重複の各パターンで、想定通りエラー画面になることを確認した。
- テスト後、`questions.json` を正常形に戻し、通常通りクイズが動作することを確認した。

### 学んだこと

- 同じ処理の組み合わせが複数箇所に出てくる場合は、関数に切り出すと読みやすくなる。
- `showLoadError()` はエラーメッセージを表示するだけの関数である。
- `showErrorScreen()` は、画面モードをエラー状態に切り替えたうえで、エラーメッセージを表示する関数である。
- `return` は書いた関数だけを終了するため、`showErrorScreen()` の中ではなく、`loadQuestionsData()` 側に残す必要がある。
- エラー処理を関数化すると、画面モード切り替えとメッセージ表示の抜け漏れを防ぎやすくなる。

### 次にやること

- Week4 Day5-6 に進む。
- 次は、読み込み成功時の処理も整理し、正常にクイズを開始する処理を関数としてまとめられるか検討する。

## 2026-06-26

### Week4 Day5-6 完了

### 完了したこと

- `LOADING` / `LOAD_ERROR` の画面制御を `updateScreenByQuizMode()` に集約した。
- `loadQuestionsData()` の冒頭に残っていた `hideNextButton()`、`hideChoicesArea()`、`disableAnswerButtons()` を削除した。
- 読み込み成功時に残っていた `showNextButton()`、`showChoicesArea()` も、`setQuizMode(QUIZ_MODE.ANSWERING)` による表示制御と重複するため削除した。
- JSONファイル名ミス、空配列、問題形式エラー、id重複の各パターンで、エラー画面が想定通り表示されることを確認した。
- テスト後、`questions.json` を正常形に戻し、クイズを3周できることを確認した。

### 学んだこと

- 画面表示の制御を `quizMode` と `updateScreenByQuizMode()` に寄せると、個別に `style.display` を操作する必要が減る。
- 同じ表示制御を複数の場所で行うと、どこが画面状態の責任を持っているのか分かりにくくなる。
- `hideNextButton()` や `hideChoicesArea()` のような個別関数は便利だが、状態管理が整理された後は不要になる場合がある。
- 関数化は常に正解ではなく、再利用性・責務の分離・可読性のどれに効くのかを判断して行う必要がある。
- 一度しか出てこない短い処理は、無理に関数化しない方が読みやすい場合がある。

### 次にやること

- Week4 Day5-7 に進む。
- Day5全体の棚卸しを行い、`LOADING`、`LOAD_ERROR`、`ANSWERING`、`READY_TO_RESULT`、`RESULT` の役割を整理する。

## 2026-06-26

### Week4 Day5-7 完了

### 完了したこと

- Week4 Day5 全体の棚卸しを行った。
- `LOADING`、`LOAD_ERROR`、`ANSWERING`、`READY_TO_RESULT`、`RESULT` の役割を整理した。
- `LOADING` は、画面読み込み後に `questions.json` を読み込んでいる状態であると整理した。
- `LOAD_ERROR` は、クイズ開始前ではあるが、読み込み失敗やデータ不正によりクイズを開始できないエラー状態であると整理した。
- `ANSWERING` では、問題文・進行度・選択肢・結果表示欄・解説表示欄・次へボタンを表示し、`quizScore` は非表示にすることを確認した。
- `RESULT` では、`nextButton` が「もう一度挑戦！」ボタンとして機能するため、非表示にしてはいけないことを確認した。
- `showErrorScreen()` により、`setQuizMode(QUIZ_MODE.LOAD_ERROR)` と `showLoadError(...)` の組み合わせを重複して書かずに済むようになったことを整理した。
- `hideNextButton()` や `hideChoicesArea()` は、`quizMode` による画面制御へ集約したため、読み込み処理内では不要になったことを整理した。
- 関数化するかどうかは、同じ目的を持つ処理が複数箇所に出ているか、自然な名前を付けられるか、可読性が上がるかを見て判断する必要があると確認した。
- 現在の `loadQuestionsData()` はまだ少し長いが、無理に関数化するとかえって読みづらくなる可能性があるため、この段階では大きく分割しない判断をした。
- 今後、問題データ検証がさらに増えた場合は、形式エラーの詳細を `console.error` に表示する処理が関数化候補になると整理した。

### 学んだこと

- 画面状態を `quizMode` として明示すると、読み込み中・エラー時・出題中・結果画面の違いを整理しやすい。
- エラー時には、問題データが存在する前提の表示や操作部品を隠すことで、ユーザーの混乱を防げる。
- 結果画面では、同じ `nextButton` でも通常時とは役割が変わるため、画面状態ごとの役割を確認する必要がある。
- 関数化は、繰り返しを減らすためだけでなく、処理の意味を名前で表すためにも使える。
- ただし、一度しか出てこない短い処理や、名前を付けても中身を読まないと分かりにくい処理は、無理に関数化しない方がよい。
- コードを整理する時は、「関数化できるか」ではなく、「関数化することで読みやすくなるか」を考える必要がある。

### Week4 完了

- Week4 では、外部JSON化、読み込み失敗対応、データ形式検証、id管理、id重複チェック、読み込み中・エラー時の画面状態管理を実装した。
- `questions.json` を壊した異常系テストと、正常形に戻した通常動作確認を繰り返し行った。
- データと画面状態の両方を守る実装を通じて、クイズアプリの保守性と安全性を高めた。

### 次にやること

- Week5 に進む。
- 次は、ここまで作ったクイズアプリを土台に、見た目・ユーザー操作・回答後の表示改善など、利用者にとって分かりやすいUIへ整えていく。

## 2026-06-26

### Week5 Day1-1 完了

### 完了したこと

- TypeScript導入前の準備として、`script.js` に残っていた未使用・未整理の関数を削除した。
- `hideNextButton()`、`showNextButton()`、`hideChoicesArea()`、`showChoicesArea()`、`hideQuizContentDuringLoad()`、`showQuizContentDuringLoad()` を削除した。
- 書きかけの `const quizContent =` を削除した。
- DOM取得部分のセミコロン抜けを修正した。
- `updateNextButtonText()` の `QUIZ_MODE.RESULT` 分岐を、波括弧つきの読みやすい形に整えた。
- `getQuestionValidationErrors(question)` で、`question` が `null` の場合も不正データとして扱えるようにした。
- 不要な `questionCount` を削除した。
- 正常な `questions.json` で、クイズを3周しても正しく動作することを確認した。
- `questions.json` のファイル名ミス、空配列、問題形式不正、ID重複の各ケースで、想定通りエラー表示が出ることを確認した。
- テスト後、`questions.json` を正常な状態に戻した。

### 学んだこと

- TypeScript導入前には、未使用関数や書きかけコードを整理しておく必要がある。
- 使っていない変数や関数が残っていると、型付け時に余計な判断材料が増え、学習効率が落ちる。
- `typeof null` は `"object"` になるため、外部データを検証する時は `question === null` も明示的に確認する必要がある。
- 正常系だけでなく、読み込み失敗・空配列・形式不正・ID重複などの異常系も確認すると、整理後の安全性を確かめられる。

### 次にやること

- Week5 Day1-2 に進む。
- 次は、TypeScript導入に向けて現在のJavaScriptコードの構造を棚卸しする。
- まずは型を付ける対象として、問題データ、状態変数、DOM要素、関数の引数と戻り値を分類する。
- 目的は、いきなり `script.ts` に書き換えるのではなく、どこにどの型が必要になるかを事前に見通せるようにすることである。

## 2026-06-26

### Week5 Day1-2 完了

### 完了したこと

- TypeScript化に入る前に、現在の `script.js` の値や関数を分類した。
- 問題データに関係するものとして、`questions`、`id`、`statement`、`correctAnswer`、`reasonText`、`choices`、データ検証関数、読み込み関数を挙げた。
- クイズの状態に関係するものとして、`currentQuestionIndex`、`isAnswered`、`score`、`quizMode`、`resetQuizState()` を挙げた。
- DOM要素に関係するものとして、`answerButtons`、`statement`、`result`、`reason`、`nextButton`、`progress`、`quizScore`、`questionTitle`、`choices`、`hideElements` などを挙げた。
- 関数の引数・戻り値については、まだ分類に不安があることを確認した。
- TypeScriptについて、現時点では変換、型チェック、型推論、any、関数引数の型注釈が必要になりやすいことを理解していると整理した。

### 学んだこと

- TypeScript化では、値を「問題データ」「状態」「DOM要素」「関数の引数・戻り値」に分けて見ると整理しやすい。
- `id`、`statement`、`correctAnswer`、`reasonText`、`choices` は1問分の問題データの中身である。
- `questions` は問題データの配列である。
- `quizMode` は現在の状態を表し、`QUIZ_MODE` は状態名の候補をまとめた定数である。
- DOM要素は、TypeScriptでは `null` の可能性を考える必要がある。
- 関数の型を見る時は、変数名を拾うだけでなく、「何を受け取り、何を返すか」を見る必要がある。
- 外部JSONから読み込んだデータは、読み込んだ時点では信用せず、検証後に初めて期待する形として扱う必要がある。

### 次にやること

- Week5 Day1-3 に進む。
- 次は、関数の引数と戻り値に注目して、現在の関数を「受け取る値」と「返す値」で整理する。
- 目的は、TypeScriptで `string`、`boolean`、`void`、問題データ型などを付ける前に、関数ごとの入口と出口を読めるようにすることである。

## 2026-06-26

### Week5 Day1-3 完了

### 完了したこと

- TypeScript化に備えて、現在の関数を「受け取る値」と「返す値」で整理した。
- `renderQuestion(question)` は1問分の問題データを受け取り、画面を書き換えるが、値は返さないと確認した。
- `setQuizMode(newMode)` は変更後のクイズ状態を受け取り、状態変更と画面更新を行うが、値は返さないと確認した。
- `getCurrentQuestion()` は引数を受け取らず、現在の問題データを返すと確認した。
- `hasNextQuestion()` は引数を受け取らず、次の問題があるかどうかを `true / false` で返すと確認した。
- `isCorrectAnswer(answerText, question)` は選択肢の文字列と問題データを受け取り、正解かどうかを `true / false` で返すと確認した。
- `showResult(judge, explanation)`、`showLoadError(message)`、`showErrorScreen(errorDetail)` は画面を書き換える関数であり、値は返さないと確認した。
- `isNonEmptyString(value)` と `isValidQuestion(question)` は、検証結果を `true / false` で返す関数だと確認した。
- `getQuestionValidationErrors(question)` は、検証エラーメッセージの配列を返す関数だと確認した。
- `findDuplicateQuestionIds(questions)` は、重複しているIDの配列を返す関数だと確認した。
- `loadQuestionsData()` は `async` 関数であり、通常の意味では値を返さないが、TypeScriptでは `Promise<void>` と考える必要があると確認した。

### 学んだこと

- TypeScriptで関数を見る時は、「何をする関数か」だけでなく、「何を受け取り、何を返すか」を見る必要がある。
- 画面を書き換える関数や状態を変更する関数は、処理は行うが値を返さない場合が多い。
- 値を返さない関数は、TypeScriptでは `void` として扱う候補になる。
- `true / false` を返す関数は、TypeScriptでは戻り値が `boolean` になる。
- `getQuestionValidationErrors()` のように配列を返す関数は、戻り値の中身が何の配列かを考える必要がある。
- `async` 関数は、値を返していないように見えても、TypeScriptでは `Promise<void>` のように考える必要がある。
- 外部データを検証する関数では、最初から正しい問題データと決めつけず、「問題データらしきもの」を受け取って検証すると考える必要がある。

### 次にやること

- Week5 Day1-4 に進む。
- 次は、今回整理した「受け取る値」と「返す値」を、TypeScriptの型候補に置き換える。
- まずは `string`、`number`、`boolean`、`void`、`Promise<void>`、`string[]`、問題データ型の候補を、日本語で対応させる。
- 目的は、いきなりコードに型注釈を書く前に、現在の関数へどの型が必要になるかを判断できるようにすることである。

## 2026-06-26

### Week5 Day1-5 完了

### 完了したこと

- `questions.json` の1問分を見ながら、`Question` 型に必要な項目を整理した。
- `id` は文字列なので `string` と考えた。
- `statement` は問題文の文字列なので `string` と考えた。
- `correctAnswer` は正解の文字列なので `string` と考えた。
- `reasonText` は解説文の文字列なので `string` と考えた。
- `choices` は複数の選択肢文字列を持つ配列なので `string[]` と考えた。
- `Question` はTypeScriptに最初からある型ではなく、自分で作る型名であることを確認した。

### 学んだこと

- TypeScriptでは、1問分のデータの形に名前を付けて、自作型として扱える。
- `string` は1つの文字列を表す。
- `string[]` は文字列が複数入った配列を表す。
- `choices` は1つの文字列ではなく、複数の選択肢を配列として持つため、`string` ではなく `string[]` になる。
- `Question` は1問分の問題データの型であり、`Question[]` は問題データが複数入った配列の型である。

### 次にやること

- Week5 Day1-6 に進む。
- 次は、実際に `script.js` の冒頭付近へ `Question` 型の形をTypeScriptとして書く準備をする。
- まずは `type Question = { ... }` の書き方を確認し、`id`、`statement`、`correctAnswer`、`reasonText`、`choices` に型を付ける。
- 目的は、問題データの形をTypeScript上で明示できるようにすることである。

## 2026-06-26

### Week5 Day1-6 完了

### 完了したこと

- `Question` 型を TypeScript の形で書く練習をした。
- `type Question = { ... };` の形で、1問分の問題データの型を表せることを確認した。
- `id`、`statement`、`correctAnswer`、`reasonText` は `string` とした。
- `choices` は複数の選択肢文字列を持つ配列なので `string[]` とした。
- `Question` はTypeScriptに最初から用意されている型ではなく、自分で定義する型名であることを確認した。
- `Question` は1問分の問題データ、`Question[]` は問題データが複数入った配列を表すことを確認した。

### 学んだこと

- `type` を使うと、オブジェクトの形に名前を付けられる。
- `Question` 型は、実際の値を書くものではなく、問題データが持つべき項目と型を示すものである。
- `id: string;` は、`id` には文字列が入るべきだという意味である。
- `choices: string[];` は、`choices` が文字列の配列であることを示す。
- TypeScriptでは、配列を扱う時に「何が入った配列か」まで考える必要がある。

### 次にやること

- Week5 Day1-7 に進む。
- 次は、現在の `script.js` をすぐ全面的に書き換えるのではなく、TypeScript用ファイルを作る準備に入る。
- `script.ts` を作成し、まずは `Question` 型だけを書いて、TypeScriptの型定義を実ファイル上で扱う入口に進む。
- 目的は、JavaScriptの実行コードとTypeScriptの型定義を混同せず、段階的にTypeScript化を始めることである。

## 2026-06-26

### Week5 Day1-7 完了

### 完了したこと

- TypeScript化の入口として、`script.ts` を新規作成した。
- `script.ts` の先頭に `Question` 型を定義した。
- `Question` 型には、`id`、`statement`、`correctAnswer`、`reasonText`、`choices` を持たせた。
- `id`、`statement`、`correctAnswer`、`reasonText` は `string` とした。
- `choices` は複数の選択肢文字列を持つため、`string[]` とした。
- 既存の `script.js` はまだ変更しなかった。
- `index.html` の `script src` もまだ変更しなかった。

### 学んだこと

- TypeScript化では、いきなり既存のJavaScriptを全面的に書き換えず、型定義から段階的に始められる。
- `script.ts` はTypeScriptとして書く元ファイルであり、ブラウザが直接実行する完成ファイルではない。
- 現時点では、ブラウザで動いているのは従来通り `script.js` である。
- `Question` 型を先に作ることで、今後 `questions`、`renderQuestion()`、`isCorrectAnswer()` などに型を付ける準備ができる。

### 次にやること

- Week5 Day1-8 に進む。
- 次は、TypeScriptをJavaScriptへ変換するための準備を行う。
- まずは TypeScript コンパイラを使える状態か確認し、必要に応じてプロジェクトに TypeScript を導入する。
- 目的は、`script.ts` を書いた後に、実行用の `script.js` へ変換できる流れを作ることである。

## 2026-06-26

### Week5 Day1-8 完了

### 完了したこと

- TypeScriptコンパイラを使う準備を行った。
- `npx tsc --version` 実行時に `tsc@2.0.4` のインストールを求められたため、その場ではインストールせず中止した。
- `tsc` パッケージではなく、TypeScript本体である `typescript` パッケージを導入する必要があることを確認した。
- `npm install --save-dev typescript` を実行し、TypeScriptを開発用依存として追加した。
- `npx tsc --version` を再実行し、TypeScriptコンパイラのバージョンが表示されることを確認した。

### 学んだこと

- TypeScriptのコンパイラ `tsc` は、`typescript` パッケージに含まれている。
- `npx tsc --version` で `tsc@2.0.4` のインストールを求められた場合、そのまま進めるのは適切ではない。
- プロジェクトでは、`npm install --save-dev typescript` により TypeScript を開発用依存として導入する。
- `npx tsc --version` でバージョンが表示されれば、TypeScriptをJavaScriptへ変換する準備ができている。
- `node_modules/` は依存パッケージ本体が入るフォルダであり、通常は Git 管理に含めない。

### 次にやること

- Week5 Day1-9 に進む。
- 次は、`node_modules/` が Git 管理に入らないように `.gitignore` を確認する。
- その後、TypeScript導入により追加された `package.json`、`package-lock.json` などを適切にコミットする。
- 目的は、TypeScriptを導入した状態を、不要な依存フォルダを含めずにリポジトリへ記録することである。

## 2026-06-26

### Week5 Day1-10 完了

### 完了したこと

- `npx tsc --init` を実行し、`tsconfig.json` を作成した。
- `npx tsc --showConfig` を実行し、TypeScriptが設定を読み込めることを確認した。
- `index.html` は変更せず、引き続き `script.js` を読み込む状態のままにした。
- `script.js` も変更しなかった。

### 学んだこと

- `tsconfig.json` は、TypeScriptをどの設定でコンパイルするかを管理する設定ファイルである。
- `npx tsc --init` により、TypeScript用の設定ファイルを作成できる。
- `npx tsc --showConfig` により、TypeScriptが認識している設定を確認できる。
- TypeScript導入時も、既存の動作している `script.js` をすぐ置き換えず、段階的に進める方が安全である。

### 次にやること

- Week5 Day1-11 に進む。
- 次は、TypeScriptをコンパイルした時に既存の `script.js` を上書きしないよう、出力先を確認・設定する。
- 目的は、`script.ts` を安全に JavaScript へ変換できる準備を整えることである。
